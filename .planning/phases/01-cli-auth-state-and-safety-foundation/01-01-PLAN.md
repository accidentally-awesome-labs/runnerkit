---
phase: 01-cli-auth-state-and-safety-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - cmd/runnerkit/main.go
  - internal/cli/root.go
  - internal/cli/up.go
  - internal/cli/exit.go
  - internal/ui/output.go
  - internal/ui/prompt.go
  - internal/redact/redact.go
  - internal/redact/redact_test.go
  - internal/testsupport/golden.go
  - internal/cli/up_test.go
  - internal/ui/output_test.go
autonomous: true
requirements:
  - CLI-01
  - CLI-02
  - STATE-02
user_setup: []
must_haves:
  truths:
    - "Developer can run the local runnerkit binary and see stable help/version output."
    - "Developer can start runnerkit up and see prerequisites before any state write is attempted."
    - "Automation callers can use --json, --non-interactive, --dry-run, --yes, and --no-color with deterministic output and exit codes."
    - "Secrets registered with the redaction layer never appear raw in human output, JSON output, or logs."
  artifacts:
    - path: "go.mod"
      provides: "Go module definition for RunnerKit"
      contains: "module"
    - path: "cmd/runnerkit/main.go"
      provides: "runnerkit binary entrypoint"
      contains: "cli.NewRootCommand"
    - path: "internal/cli/root.go"
      provides: "Cobra root command, persistent flags, and version command wiring"
      contains: "runnerkit"
    - path: "internal/cli/up.go"
      provides: "runnerkit up guided foundation wizard scaffold"
      contains: "Will not install a runner in Phase 1"
    - path: "internal/ui/output.go"
      provides: "Human/JSON terminal output renderer"
      contains: "redactions_applied"
    - path: "internal/redact/redact.go"
      provides: "Central sensitive-value redaction package"
      contains: "<redacted:github-token>"
  key_links:
    - from: "cmd/runnerkit/main.go"
      to: "internal/cli/root.go"
      via: "main executes cli.NewRootCommand"
      pattern: "cli\.NewRootCommand"
    - from: "internal/cli/up.go"
      to: "internal/ui/output.go"
      via: "up command renders all wizard steps through the shared renderer"
      pattern: "ui\."
    - from: "internal/ui/output.go"
      to: "internal/redact/redact.go"
      via: "output renderer sanitizes before writing human or JSON output"
      pattern: "redact\."
---

<objective>
Create RunnerKit's executable CLI shell, terminal output contract, prompt abstraction, `runnerkit up` foundation wizard scaffold, automation flags, exit-code conventions, and minimal redaction layer.

Purpose: Phase 1 cannot safely add GitHub auth or state persistence until the binary, output surfaces, prompt/no-TTY behavior, and redaction are stable. This plan implements D-01, D-02, D-03, D-04, D-13, D-14, and the first redaction slice of STATE-02.

Output: A runnable Go/Cobra `runnerkit` binary with `up` and `version` commands, deterministic human/JSON output, no-TTY handling, test support, and central redaction tests.
</objective>

<execution_context>
@.pi/gsd/workflows/execute-plan.md
@.pi/gsd/templates/summary.md
</execution_context>

<context>
@GEMINI.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-cli-auth-state-and-safety-foundation/01-CONTEXT.md
@.planning/phases/01-cli-auth-state-and-safety-foundation/01-RESEARCH.md
@.planning/phases/01-cli-auth-state-and-safety-foundation/01-UI-SPEC.md
@.planning/phases/01-cli-auth-state-and-safety-foundation/01-VALIDATION.md

<interfaces>
This is a greenfield repository with no application source yet. Create these contracts for downstream plans:

```go
// internal/cli/root.go
func NewRootCommand(deps Dependencies) *cobra.Command

type Dependencies struct {
    Version string
    In      io.Reader
    Out     io.Writer
    Err     io.Writer
    TTY     ui.TerminalCapabilities
    Prompts ui.Prompter
    Clock   func() time.Time
}
```

```go
// internal/ui/output.go
type Format string
const (
    FormatHuman Format = "human"
    FormatJSON  Format = "json"
)

type TerminalCapabilities struct {
    StdinTTY  bool
    StdoutTTY bool
    Color     bool
    ASCII     bool
    Width     int
}

type Renderer struct { /* writer + format + redactor */ }
func (r *Renderer) Step(current, total int, title string, lines ...Line) error
func (r *Renderer) Warning(title string, body []string, next string) error
func (r *Renderer) JSON(v any) error
```

```go
// internal/redact/redact.go
type Kind string
const (
    GitHubToken Kind = "github-token"
    RunnerRegistrationToken Kind = "runner-registration-token"
    RunnerRemovalToken Kind = "runner-removal-token"
    SSHPrivateKey Kind = "ssh-private-key"
    ProviderCredential Kind = "provider-credential"
    MachineRef Kind = "machine-ref"
)

type Redactor struct { /* registered values and pattern filters */ }
func New() *Redactor
func (r *Redactor) Register(kind Kind, value string)
func (r *Redactor) String(input string) string
func (r *Redactor) JSONBytes(input []byte) []byte
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Initialize Go module, Cobra root command, version command, and exit-code mapping</name>
  <files>go.mod, cmd/runnerkit/main.go, internal/cli/root.go, internal/cli/exit.go</files>
  <read_first>GEMINI.md, .planning/phases/01-cli-auth-state-and-safety-foundation/01-RESEARCH.md, .planning/phases/01-cli-auth-state-and-safety-foundation/01-UI-SPEC.md, go.mod (if present), cmd/runnerkit/main.go (if present), internal/cli/root.go (if present), internal/cli/exit.go (if present)</read_first>
  <behavior>
    - `go run ./cmd/runnerkit --help` exits 0 and prints `RunnerKit` plus the `up` command.
    - `go run ./cmd/runnerkit version --json` writes JSON only to stdout with keys `ok`, `command`, `version`, and `redactions_applied`.
    - Invalid flags return exit code 2 through a typed `ExitError` mapping.
  </behavior>
  <action>Initialize the Go module for RunnerKit and add Cobra. Use module path `github.com/salar/runnerkit` unless `git remote get-url origin` clearly identifies a different GitHub owner/repo, in which case use that GitHub module path. Create `cmd/runnerkit/main.go` that calls `cli.NewRootCommand(cli.Dependencies{Version: version, In: os.Stdin, Out: os.Stdout, Err: os.Stderr, Clock: time.Now})` and exits with the mapped code from `internal/cli/exit.go`. Create `internal/cli/root.go` with root command `runnerkit`, short description `Prepare and manage GitHub Actions self-hosted runners`, persistent flags `--json`, `--no-color`, and a `version` subcommand. Implement exit code constants exactly: success 0, unexpected 1, invalid input 2, GitHub auth 3, safety gate 4, state IO 5, input required 6, canceled 130. This implements CLI-01 and D-04 without adding runner install behavior.</action>
  <verify>
    <automated>go test ./...</automated>
  </verify>
  <acceptance_criteria>
    - `go.mod` contains `github.com/spf13/cobra`.
    - `cmd/runnerkit/main.go` contains `cli.NewRootCommand`.
    - `internal/cli/root.go` contains `Use: "runnerkit"` and `Use: "version"`.
    - `internal/cli/exit.go` contains `ExitInputRequired = 6` and `ExitCanceled = 130`.
    - `go run ./cmd/runnerkit --help` exits 0.
    - `go run ./cmd/runnerkit version --json` outputs JSON containing `"redactions_applied":true` and no ANSI escape sequences.</acceptance_criteria>
  <done>The repository has a buildable `runnerkit` binary skeleton with deterministic root/version behavior and typed exit codes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement terminal renderer, prompt abstraction, JSON contract, and central redaction</name>
  <files>internal/ui/output.go, internal/ui/prompt.go, internal/redact/redact.go, internal/redact/redact_test.go, internal/ui/output_test.go, internal/testsupport/golden.go</files>
  <read_first>.planning/phases/01-cli-auth-state-and-safety-foundation/01-UI-SPEC.md, .planning/phases/01-cli-auth-state-and-safety-foundation/01-VALIDATION.md, internal/ui/output.go (if present), internal/ui/prompt.go (if present), internal/redact/redact.go (if present), internal/redact/redact_test.go (if present), internal/ui/output_test.go (if present), internal/testsupport/golden.go (if present)</read_first>
  <behavior>
    - Human renderer uses UI-SPEC glyphs `✓`, `!`, `✗`, `?`, `→`, `•` with ASCII fallbacks `OK`, `WARNING`, `ERROR`, `PROMPT`, `NEXT`, `-`.
    - `--json` renderer writes JSON only and includes `redactions_applied: true` on success and error payloads.
    - Redactor replaces all registered sensitive values and known token-like fields in strings and JSON bytes.
  </behavior>
  <action>Create `internal/ui` with a renderer that supports `FormatHuman` and `FormatJSON`, stdout/stderr separation, terminal capabilities, `NO_COLOR`, `CLICOLOR=0`, `TERM=dumb`, `--no-color`, ASCII fallback, and 64-cell prose wrapping per the UI-SPEC. Create `Prompter` interface with `Confirm(ctx, Prompt) (bool, error)` and `Select(ctx, Prompt, []Option) (string, error)`; no concrete rich TUI dependency is required yet. Create `internal/redact` with `Kind` constants and replacement strings exactly `<redacted:github-token>`, `<redacted:runner-registration-token>`, `<redacted:runner-removal-token>`, `<redacted:ssh-private-key>`, `<redacted:provider-credential>`, and `<redacted:machine-ref>`. Ensure renderer calls redaction before writing human output, JSON, warnings, and errors. Add golden/test helpers that compare complete output strings without ANSI when color is disabled. This implements STATE-02 early so Plan 01-02 cannot leak auth tokens.</action>
  <verify>
    <automated>go test ./internal/redact ./internal/ui ./...</automated>
  </verify>
  <acceptance_criteria>
    - `internal/redact/redact.go` contains `<redacted:github-token>` and `<redacted:runner-registration-token>`.
    - `internal/redact/redact_test.go` contains test cases for `ghp_example`, `github_pat_`, `registration-token-secret`, `remove-token-secret`, `-----BEGIN OPENSSH PRIVATE KEY-----`, and `HCLOUD_TOKEN`.
    - `internal/ui/output.go` contains `redactions_applied`.
    - `internal/ui/output_test.go` asserts JSON output starts with `{` and contains no `\x1b[`.
    - `go test ./internal/redact ./internal/ui ./...` exits 0.</acceptance_criteria>
  <done>All CLI output paths use a shared renderer and redactor, with tests proving sensitive values are masked before display.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add runnerkit up foundation wizard scaffold and automation flags</name>
  <files>internal/cli/up.go, internal/cli/up_test.go, internal/cli/root.go</files>
  <read_first>.planning/phases/01-cli-auth-state-and-safety-foundation/01-CONTEXT.md, .planning/phases/01-cli-auth-state-and-safety-foundation/01-UI-SPEC.md, internal/cli/up.go (if present), internal/cli/up_test.go (if present), internal/cli/root.go</read_first>
  <behavior>
    - `runnerkit up --dry-run --repo owner/name --yes` prints all six Phase 1 wizard steps and says no runner is installed.
    - `runnerkit up --non-interactive` without `--repo` exits 6 with missing flag remediation.
    - The state write prompt defaults to no and no persistence occurs in this plan.
  </behavior>
  <action>Create `runnerkit up` in `internal/cli/up.go` as the Phase 1 guided foundation command favored by D-02. Add flags exactly `--repo owner/name`, `--yes`, `--non-interactive`, `--dry-run`, and `--allow-public-repo-risk` placeholder; wire `--json` and `--no-color` from the root. Implement the wizard order from D-13 exactly as step titles: `Welcome`, `Prerequisites`, `Repo/auth`, `Safety checks`, `State preview`, `Next steps`. The Welcome and Next steps copy must include `Phase 1 does not install a runner yet` and `Will not install a runner in Phase 1` per D-14 and UI-SPEC. Implement D-03 by rendering a plan/checklist before the placeholder save step and asking `Save this foundation state? [y/N]`; the default is false. Implement D-04 by making `--dry-run` require no confirmation, making no-TTY prompt requirements exit with code 6, and making `--json` return the UI-SPEC shape with `runner_installed: false`, `warnings: []`, `next_steps`, and `redactions_applied: true`. Use fake placeholder repo/auth/safety/state providers for now; Plan 01-02 and 01-03 replace them with real adapters.</action>
  <verify>
    <automated>go test ./internal/cli ./...</automated>
  </verify>
  <acceptance_criteria>
    - `internal/cli/up.go` contains all six strings: `Welcome`, `Prerequisites`, `Repo/auth`, `Safety checks`, `State preview`, `Next steps`.
    - `internal/cli/up.go` contains `Phase 1 does not install a runner yet` and `Will not install a runner in Phase 1`.
    - `internal/cli/up.go` contains `Save this foundation state? [y/N]`.
    - `internal/cli/up.go` registers flags `repo`, `yes`, `non-interactive`, `dry-run`, and `allow-public-repo-risk`.
    - `internal/cli/up_test.go` contains `ExitInputRequired` and verifies missing `--repo` in non-interactive mode.
    - `go test ./internal/cli ./...` exits 0.</acceptance_criteria>
  <done>`runnerkit up` is a safe, honest, automation-friendly Phase 1 wizard scaffold ready for real repo/auth/state adapters.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go test ./...` exits 0.
- [ ] `go run ./cmd/runnerkit --help` exits 0 and lists `up`.
- [ ] `go run ./cmd/runnerkit version --json` emits JSON only with `redactions_applied: true`.
- [ ] `go run ./cmd/runnerkit up --dry-run --repo owner/name --yes --no-color` prints `Will not install a runner in Phase 1`.
- [ ] `go run ./cmd/runnerkit up --non-interactive --no-color` exits with code 6.
</verification>

<success_criteria>

- CLI-01 covered by a buildable/runnable local `runnerkit` binary.
- CLI-02 covered by a guided setup scaffold that explains prerequisites before changes.
- STATE-02 partially covered by central redaction wired into all current output paths.
- D-01, D-02, D-03, D-04, D-13, and D-14 are implemented or scaffolded without implying a runner exists.
  </success_criteria>

<output>
After completion, create `.planning/phases/01-cli-auth-state-and-safety-foundation/01-01-SUMMARY.md`.
</output>
