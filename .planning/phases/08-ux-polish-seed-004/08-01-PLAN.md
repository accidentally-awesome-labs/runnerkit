# Phase 08-01 — SEED-004 UX polish (implementation summary)

Delivered in code:

- **Stage JSON** — [`internal/ux/stage`](internal/ux/stage) maps observed health + doctor deep checks to `stage` strings; `status --json` merges [`nextaction.MergePayload`](../../internal/ux/nextaction/nextaction.go); `doctor --json` adds `schema_version` + `stage` via [`ApplySchemaAndStage`](../../internal/ux/nextaction/nextaction.go) while preserving existing `next_actions` shape.
- **Boxed commands** — [`internal/ui/box.go`](../../internal/ui/box.go) + tests.
- **Checklists** — [`internal/ui/checklist.go`](../../internal/ui/checklist.go), [`internal/ux/checkliststore`](../../internal/ux/checkliststore), BYO progress in [`internal/cli/byo_checklist.go`](../../internal/cli/byo_checklist.go) wired from [`up.go`](../../internal/cli/up.go).
- **First-run wizard** — bare `runnerkit` runs [`wizard.go`](../../internal/cli/wizard.go) when no saved repos; otherwise `help`. JSON path returns `next_actions` without requiring a TTY.
- **`--explain` / `--unicode`** — root persistent flags; explain blocks for `init` and BYO `up` path; wizard uses Unicode box when requested.
- **`doctor --fix` / `--ignore` / config** — [`doctor_fix.go`](../../internal/cli/doctor_fix.go), [`userconfig.go`](../../internal/cli/userconfig.go); `runner_version_stale` triggers `upgrade-runner` (with confirm unless `--yes`).
- **Smoke** — [`scripts/smoke/assert-doctor-json-contract.sh`](../../scripts/smoke/assert-doctor-json-contract.sh) requires `schema_version` and `stage`.

Verification: `go test ./...`, `make smoke-live` (maintainer), BYO smoke with `assert-doctor-json-contract.sh`.
