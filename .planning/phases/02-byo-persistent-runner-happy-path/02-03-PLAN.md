---
phase: 02-byo-persistent-runner-happy-path
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - internal/github/types.go
  - internal/github/runners.go
  - internal/github/runners_test.go
  - internal/github/service.go
  - internal/github/service_test.go
  - internal/labels/labels.go
  - internal/labels/labels_test.go
  - internal/state/schema.go
  - internal/state/state_test.go
  - internal/cli/up.go
  - internal/cli/up_test.go
  - internal/cli/up_integration_test.go
autonomous: true
requirements_addressed: [GH-02, GH-04, GH-05, CLI-04, RUN-01]
requirements:
  - GH-02
  - GH-04
  - GH-05
  - CLI-04
  - RUN-01
user_setup: []
must_haves:
  truths:
    - "RunnerKit registers a repository-scoped persistent GitHub Actions runner without asking the developer to copy GitHub setup commands."
    - "RunnerKit detects same-name repository runners before registration and refuses silent duplicates."
    - "RunnerKit verifies both remote service active state and GitHub online runner inventory before declaring setup complete."
    - "Saved state records runner ID, name, labels, mode `persistent`, machine target, install path, work dir, service name, and host fingerprint, but no registration token."
    - "Completion output includes runner name, labels, machine target, service status, state path, and exact copy-paste `runs-on` guidance without editing workflow YAML."
  artifacts:
    - path: "internal/github/runners.go"
      provides: "Repository self-hosted runner inventory/status/delete API wrapper"
      contains: "ListRunners"
    - path: "internal/cli/up.go"
      provides: "End-to-end BYO registration orchestration and completion summary"
      contains: "BYO runner ready"
    - path: "internal/state/schema.go"
      provides: "Persisted runner ID and BYO machine/service metadata"
      contains: "GitHubRunnerID"
    - path: "internal/labels/labels.go"
      provides: "Detected arch-aware RunnerKit label/snippet generation"
      contains: "WorkflowSnippet"
  key_links:
    - from: "internal/cli/up.go"
      to: "internal/github/runners.go"
      via: "up lists repo runners before registration and polls runner status after service start"
      pattern: "ListRunners"
    - from: "internal/cli/up.go"
      to: "internal/bootstrap/install.go"
      via: "up requests a fresh registration token immediately before configure_runner"
      pattern: "CreateRegistrationToken|bootstrap\.Apply"
    - from: "internal/cli/up.go"
      to: "internal/state/store.go"
      via: "up saves complete BYO runner state only after GitHub and service verification pass"
      pattern: "SaveRepository"
    - from: "internal/cli/up.go"
      to: "internal/labels/labels.go"
      via: "completion summary prints exact RunnerKit label snippet"
      pattern: "RunsOnYAML"
---

<objective>
Connect the preflight/bootstrap path to real repository-scoped GitHub runner registration, runner inventory verification, state persistence, and final completion guidance.

Purpose: After Plan 02-02 can install/configure the runner service with an ephemeral token, Phase 2 needs to prove the runner is visible and online in GitHub with safe labels before telling the developer the happy path is complete.

Output: GitHub runner inventory APIs, duplicate-name protection, end-to-end fake setup tests, persistent state updates, and human/JSON completion summaries with copy-paste workflow guidance.
</objective>

<execution_context>
@.pi/gsd/workflows/execute-plan.md
@.pi/gsd/templates/summary.md
</execution_context>

<context>
@GEMINI.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-byo-persistent-runner-happy-path/02-CONTEXT.md
@.planning/phases/02-byo-persistent-runner-happy-path/02-RESEARCH.md
@.planning/phases/02-byo-persistent-runner-happy-path/02-VALIDATION.md
@.planning/phases/02-byo-persistent-runner-happy-path/02-01-PLAN.md
@.planning/phases/02-byo-persistent-runner-happy-path/02-02-PLAN.md
@internal/github/client.go
@internal/github/service.go
@internal/github/tokens.go
@internal/bootstrap/install.go
@internal/labels/labels.go
@internal/state/schema.go
@internal/cli/up.go

<interfaces>
Extend the GitHub adapter with repository runner inventory:

```go
// internal/github/runners.go
type Runner struct {
    ID int64
    Name string
    OS string
    Status string // online/offline
    Busy bool
    Labels []string
}

type RunnerManager interface {
    CreateRegistrationToken(ctx context.Context, repo Repo) (RunnerToken, error)
    CreateRemovalToken(ctx context.Context, repo Repo) (RunnerToken, error)
    ListRunners(ctx context.Context, repo Repo) ([]Runner, error)
    DeleteRunner(ctx context.Context, repo Repo, runnerID int64) error
}
```

The CLI-facing `GitHubService` should include runner-management methods only after tests/fakes are updated.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add repository runner inventory API and duplicate-name detection</name>
  <files>internal/github/types.go, internal/github/runners.go, internal/github/runners_test.go, internal/github/client.go, internal/github/service.go, internal/github/service_test.go, internal/cli/up.go, internal/cli/up_test.go</files>
  <read_first>.planning/phases/02-byo-persistent-runner-happy-path/02-RESEARCH.md, internal/github/types.go, internal/github/client.go, internal/github/tokens.go, internal/github/service.go, internal/github/service_test.go, internal/github/runners.go (if present), internal/github/runners_test.go (if present), internal/cli/up.go, internal/cli/up_test.go</read_first>
  <behavior>
    - `ListRunners` parses GitHub response `total_count` and `runners` with ID, name, OS, status, busy, and label names.
    - Existing runner with same name and status `online` blocks setup with error code `runner_name_conflict`.
    - Existing runner with same name and status `offline` blocks setup with remediation to remove stale runner or rerun after Phase 3 cleanup support; silent duplicate registration is not allowed.
  </behavior>
  <action>Create `internal/github/runners.go` with `Runner`, `RunnerManager`, `ListRunners`, `DeleteRunner`, and helper `FindRunnerByName(runners []Runner, name string) (Runner, bool)`. Implement client method `ListRunners(ctx, repo)` using `GET /repos/{owner}/{repo}/actions/runners` and parsing JSON shaped `{"total_count":1,"runners":[{"id":123,"name":"runnerkit-owner-repo-local","os":"linux","status":"online","busy":false,"labels":[{"name":"self-hosted"},{"name":"runnerkit"}]}]}`. Implement `DeleteRunner(ctx, repo, runnerID)` using `DELETE /repos/{owner}/{repo}/actions/runners/{runner_id}` for future replacement, but the Phase 2 CLI should not auto-delete duplicates. Extend `github.Service` to expose `CreateRegistrationToken`, `CreateRemovalToken`, `ListRunners`, and `DeleteRunner` by using its discovered credential and `Client`. Extend `cli.GitHubService` and all CLI test fakes accordingly. In `runUp`, before requesting a new registration token, call `ListRunners` and block if `FindRunnerByName` matches the planned runner name. Render error code `runner_name_conflict` with remediation `Remove or rename the existing GitHub runner runnerkit-owner-repo-local, then re-run runnerkit up.` for online and offline duplicates alike. Do not implement automatic stale deletion in this plan.</action>
  <verify>
    <automated>go test ./internal/github ./internal/cli ./...</automated>
  </verify>
  <acceptance_criteria>
    - `internal/github/runners.go` contains `type Runner struct` and `func FindRunnerByName`.
    - `internal/github/runners.go` contains `/actions/runners` and `DeleteRunner`.
    - `internal/github/runners_test.go` contains `runnerkit-owner-repo-local`, `status":"online"`, and `labels` fixture assertions.
    - `internal/cli/up.go` contains `runner_name_conflict`.
    - `internal/cli/up_test.go` asserts duplicate runner blocks before `CreateRegistrationToken` by checking token call count `0`.
    - `go test ./internal/github ./internal/cli ./...` exits 0.</acceptance_criteria>
  <done>RunnerKit can inspect repository self-hosted runners and prevent duplicate runner names before registration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Orchestrate just-in-time registration token, bootstrap, online polling, and state save</name>
  <files>internal/cli/up.go, internal/cli/up_test.go, internal/cli/up_integration_test.go, internal/state/schema.go, internal/state/state_test.go</files>
  <read_first>.planning/phases/02-byo-persistent-runner-happy-path/02-CONTEXT.md, .planning/phases/02-byo-persistent-runner-happy-path/02-RESEARCH.md, internal/cli/up.go, internal/cli/up_test.go, internal/cli/up_integration_test.go, internal/github/runners.go, internal/github/tokens.go, internal/bootstrap/install.go, internal/preflight/checks.go, internal/state/schema.go, internal/state/state_test.go, internal/state/store.go, internal/labels/labels.go</read_first>
  <behavior>
    - Registration token is requested only after repo safety, host-key trust, preflight pass, duplicate check, and install confirmation.
    - `runnerkit up` polls GitHub runner inventory until the planned runner appears `online` with all RunnerKit labels or until timeout.
    - Successful setup saves complete BYO state with `Machine.Kind` exactly `byo-ssh`, `Provider.Kind` exactly `byo`, and `Runner.Mode` exactly `persistent`.
    - Failed online verification does not save successful state and prints remediation to check service/network/GitHub runner UI.
  </behavior>
  <action>Refactor `runUp` into a clear BYO orchestration sequence without moving business logic into Cobra: resolve repo, verify auth, read repo metadata, evaluate safety, resolve SSH target, verify host key, run preflight, build labels from detected OS/arch, list runners for duplicate name, render/confirm bootstrap plan, request fresh registration token, call `bootstrap.Apply`, verify remote service, poll `ListRunners` every `2 * time.Second` up to `60 * time.Second` using injectable clock/sleeper or a test hook, save state, render completion. Add an internal helper `runnerOnlineWithLabels(runners []github.Runner, name string, labels []string) (github.Runner, bool)` that requires `Status == "online"` and all labels present. Populate `RepositoryState` on success with `Runner.Name`, `Runner.Labels`, `Runner.WorkflowSnippet`, `Runner.Mode: "persistent"`, detected `Runner.OS`, detected `Runner.Arch`, `Machine.Kind: "byo-ssh"`, `Machine.HostRef: target.Display()`, `Machine.User`, `Machine.Port`, `Machine.InstallPath`, `Machine.WorkDir`, `Machine.ServiceName`, host-key fields from Plan 02-01, `Provider.Kind: "byo"`, `Cleanup.GitHubRunnerID` from the online runner ID, and cleanup managed paths exactly install path plus `/var/lib/runnerkit`. Ensure no `RunnerToken.Token` value is stored in any state struct. Error code for timeout is `runner_online_timeout` with remediation `Check the remote service status and GitHub repository Actions runner settings, then re-run runnerkit up.`.</action>
  <verify>
    <automated>go test ./internal/cli ./internal/state ./...</automated>
  </verify>
  <acceptance_criteria>
    - `internal/cli/up.go` contains `runnerOnlineWithLabels`.
    - `internal/cli/up.go` contains `runner_online_timeout`.
    - `internal/cli/up.go` contains `2 * time.Second` and `60 * time.Second`.
    - `internal/cli/up_integration_test.go` contains a successful fake BYO setup test asserting saved JSON contains `"kind":"byo-ssh"`, `"mode":"persistent"`, and `"github_runner_id":123`.
    - `internal/cli/up_integration_test.go` asserts saved JSON does not contain fake token `registration-token-secret-12345`.
    - `internal/state/state_test.go` asserts `MachineRef` round-trips `Port`, `HostKeyFingerprint`, `InstallPath`, `WorkDir`, and `ServiceName`.
    - `go test ./internal/cli ./internal/state ./...` exits 0.</acceptance_criteria>
  <done>RunnerKit performs repository-scoped runner registration, verifies the runner online with labels, and persists complete secret-free BYO state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render completion summary and exact workflow guidance in human and JSON output</name>
  <files>internal/cli/up.go, internal/cli/up_test.go, internal/labels/labels.go, internal/labels/labels_test.go, internal/ui/output.go, internal/ui/output_test.go</files>
  <read_first>.planning/phases/02-byo-persistent-runner-happy-path/02-CONTEXT.md, .planning/phases/02-byo-persistent-runner-happy-path/02-RESEARCH.md, internal/cli/up.go, internal/cli/up_test.go, internal/labels/labels.go, internal/labels/labels_test.go, internal/ui/output.go, internal/ui/output_test.go</read_first>
  <behavior>
    - Human completion has title `BYO runner ready` and lists runner name, machine target, service name, labels, state path, and next workflow step.
    - JSON completion contains keys `runner_installed:true`, `runner_name`, `labels`, `machine_target`, `service_name`, `workflow_snippet`, `github_runner_id`, `state_path`, `next_steps`, and `redactions_applied:true`.
    - The recommended snippet remains an array and never recommends `runs-on: self-hosted` alone.
    - RunnerKit does not create or edit `.github/workflows/*.yml` files.
  </behavior>
  <action>Update completion rendering in `internal/cli/up.go`. Human output must include exact title `BYO runner ready`, exact label prefix `Labels: [`, exact snippet from `labels.WorkflowSnippet`, exact warning `Do not use runs-on: self-hosted alone for RunnerKit-managed runners.`, exact next step `Add the runs-on snippet above to the workflow job you want to run on this runner.`, and exact cleanup preview `Later cleanup will be handled by a future runnerkit down flow; do not delete the BYO host manually if you want RunnerKit state to stay accurate.`. JSON output must include a top-level object with `ok:true`, `command:"up"`, `runner_installed:true`, `runner_name`, `labels`, `machine_target`, `service_name`, `workflow_snippet`, `github_runner_id`, `state_path`, `warnings`, `next_steps`, and `redactions_applied:true`. Update `labels.Build` to accept detected arch from Plan 02-01 (`x64` or `arm64`) while preserving default `linux`, `x64`, `persistent` if no machine facts exist. Add tests that create a temporary `.github/workflows/existing.yml`, run fake setup, and assert its bytes are unchanged after `runnerkit up`.</action>
  <verify>
    <automated>go test ./internal/cli ./internal/labels ./internal/ui ./...</automated>
  </verify>
  <acceptance_criteria>
    - `internal/cli/up.go` contains `BYO runner ready`.
    - `internal/cli/up.go` contains `Add the runs-on snippet above to the workflow job you want to run on this runner.`.
    - `internal/cli/up.go` contains `runner_installed` and `github_runner_id`.
    - `internal/cli/up_test.go` asserts human output contains `runs-on: [self-hosted, runnerkit, runnerkit-owner-repo, linux, x64, persistent]`.
    - `internal/cli/up_test.go` asserts JSON output contains `"runner_installed":true` and `"redactions_applied":true`.
    - `internal/cli/up_test.go` asserts `.github/workflows/existing.yml` is unchanged after fake setup.
    - `go test ./internal/cli ./internal/labels ./internal/ui ./...` exits 0.</acceptance_criteria>
  <done>Successful BYO setup ends with clear, copy-pasteable, non-mutating workflow guidance and complete state/output metadata.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go test ./...` exits 0.
- [ ] `grep -R "runner_name_conflict" internal/cli internal/github` returns a match.
- [ ] `grep -R "BYO runner ready" internal/cli` returns a match.
- [ ] `grep -R "runner_online_timeout" internal/cli` returns a match.
- [ ] `grep -R "github_runner_id" internal/state internal/cli` returns a match.
- [ ] `grep -R "runs-on: \[self-hosted, runnerkit" internal/cli internal/labels` returns a match.
</verification>

<success_criteria>

- GH-02 is covered by just-in-time token registration through RunnerKit, not manual GitHub copy/paste.
- GH-04 and GH-05 are covered by stable labels and printed `runs-on` guidance.
- CLI-04 is covered by human/JSON completion summary with runner name, labels, machine target, and next workflow step.
- RUN-01 is covered by persisted default mode `persistent` and active service verification.
  </success_criteria>

<output>
After completion, create `.planning/phases/02-byo-persistent-runner-happy-path/02-03-SUMMARY.md`.
</output>
