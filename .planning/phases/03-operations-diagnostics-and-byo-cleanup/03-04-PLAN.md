---
phase: 03-operations-diagnostics-and-byo-cleanup
plan: "04"
type: execute
wave: 4
depends_on:
  - "03-01"
  - "03-03"
files_modified:
  - README.md
  - docs/byo-quickstart.md
  - internal/cli/root.go
  - internal/cli/down.go
  - internal/cli/down_test.go
  - internal/cli/docs_test.go
  - internal/ops/cleanup.go
  - internal/ops/cleanup_test.go
  - internal/state/schema.go
  - internal/state/store.go
  - internal/state/state_test.go
  - internal/bootstrap/script.go
  - internal/redact/redact_test.go
autonomous: true
requirements_addressed: [GH-03, CLEAN-02, CLEAN-03]
requirements:
  - GH-03
  - CLEAN-02
  - CLEAN-03
  - STATE-02
user_setup: []
must_haves:
  truths:
    - "runnerkit down is the primary BYO cleanup command; destroy remains reserved for future cloud resources."
    - "down always builds and shows an artifact plan before mutation; interactive mode asks artifact-by-artifact with default No."
    - "down --yes applies only the safe default plan: GitHub runner record, host registration, recorded service, exact recorded install path, exact recorded work dir, and matching local state."
    - "down never deletes shared users, arbitrary BYO host data, or shared /var/lib/runnerkit parents unless proven empty and exclusively RunnerKit-owned."
    - "Stale GitHub runner records can be deleted when SSH/local machine state is missing by using an explicit GitHub runner ID."
    - "Partial cleanup writes checkpoints/notes and keeps local state until required selected artifacts are complete or intentionally skipped."
  artifacts:
    - path: "internal/cli/down.go"
      provides: "runnerkit down command with dry-run, --yes, prompts, stale GitHub targeting, and JSON contract"
      contains: "Use: \"down\""
    - path: "internal/ops/cleanup.go"
      provides: "Safe BYO cleanup artifact plan and path validation"
      contains: "github_runner"
    - path: "internal/state/store.go"
      provides: "State update/remove helpers used for partial and complete cleanup"
      contains: "RemoveRepository"
    - path: "docs/byo-quickstart.md"
      provides: "BYO cleanup documentation and stale GitHub runner guidance"
      contains: "runnerkit down --repo owner/name --dry-run"
  key_links:
    - from: "internal/cli/root.go"
      to: "internal/cli/down.go"
      via: "root wires down command"
      pattern: "newDownCommand"
    - from: "internal/cli/down.go"
      to: "internal/ops/cleanup.go"
      via: "down builds a safe cleanup plan before mutation"
      pattern: 'ops\.BuildCleanupPlan'
    - from: "internal/cli/down.go"
      to: "internal/github/runners.go"
      via: "down deletes stale GitHub runner records by exact ID or safe saved identity fallback"
      pattern: "DeleteRunner"
    - from: "internal/cli/down.go"
      to: "internal/bootstrap/script.go"
      via: "host-side runner removal uses a fresh removal token and config.sh remove script"
      pattern: "RenderRemoveConfigScript"
---

<objective>
Add safe BYO cleanup and stale GitHub deregistration through `runnerkit down`.

Purpose: Developers need to remove RunnerKit-managed persistent BYO runner artifacts without deleting unrelated host data, and they need stale GitHub runner records removed even when SSH/local state is imperfect.

Output: Cleanup artifact planner, `runnerkit down` command, stale-GitHub-only targeting, host-side unregister/service/file cleanup, partial checkpoints, state removal/update behavior, tests, and docs.
</objective>

<execution_context>
@.pi/gsd/workflows/execute-plan.md
@.pi/gsd/templates/summary.md
</execution_context>

<context>
@GEMINI.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-operations-diagnostics-and-byo-cleanup/03-CONTEXT.md
@.planning/phases/03-operations-diagnostics-and-byo-cleanup/03-RESEARCH.md
@.planning/phases/03-operations-diagnostics-and-byo-cleanup/03-VALIDATION.md
@.planning/phases/03-operations-diagnostics-and-byo-cleanup/03-UI-SPEC.md
@.planning/phases/03-operations-diagnostics-and-byo-cleanup/03-01-PLAN.md
@.planning/phases/03-operations-diagnostics-and-byo-cleanup/03-03-PLAN.md
@internal/cli/root.go
@internal/cli/status.go
@internal/cli/recover.go
@internal/ops/status.go
@internal/ops/probes.go
@internal/ops/recovery.go
@internal/bootstrap/script.go
@internal/state/schema.go
@internal/state/store.go
@internal/github/runners.go
@internal/github/tokens.go
@internal/remote/executor.go
@internal/ui/output.go
@internal/redact/redact.go

<interfaces>
Add this command:

```text
runnerkit down --repo owner/name --dry-run
runnerkit down --repo owner/name
runnerkit down --repo owner/name --yes
runnerkit down --repo owner/name --github-runner-id 123 --yes
```

Flags: `--repo owner/name`, `--yes`, `--dry-run`, `--github-runner-id int64`, and `--runner-name string`. Global `--json` and `--no-color` remain inherited.

Cleanup artifact IDs:

- `github_runner`
- `host_registration`
- `systemd_service`
- `runner_files`
- `local_state`

Cleanup remote command IDs:

- `down.runner.remove`
- `down.service.uninstall`
- `down.files.remove`

JSON top-level keys: `ok`, `command`, `repo`, `dry_run`, `plan`, `results`, `partial_cleanup`, `pending`, `state_removed`, and `redactions_applied`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 03-04-01: Build safe BYO cleanup artifact plan and path validation</name>
  <files>internal/ops/cleanup.go, internal/ops/cleanup_test.go, internal/state/schema.go, internal/state/state_test.go</files>
  <read_first>.planning/phases/03-operations-diagnostics-and-byo-cleanup/03-CONTEXT.md, .planning/phases/03-operations-diagnostics-and-byo-cleanup/03-RESEARCH.md, .planning/phases/03-operations-diagnostics-and-byo-cleanup/03-UI-SPEC.md, internal/ops/cleanup.go (if present), internal/ops/cleanup_test.go (if present), internal/state/schema.go, internal/state/state_test.go, internal/testsupport/state.go, internal/labels/labels.go</read_first>
  <behavior>
    - Cleanup plan contains artifacts `github_runner`, `host_registration`, `systemd_service`, `runner_files`, and `local_state` for a complete saved BYO state.
    - Safe default excludes shared `/var/lib/runnerkit`, shared users, and provider/cloud resources.
    - Unsafe install/work paths are marked blocked and are not emitted in deletion commands.
  </behavior>
  <action>Create `internal/ops/cleanup.go` with `CleanupArtifact` constants `ArtifactGitHubRunner = "github_runner"`, `ArtifactHostRegistration = "host_registration"`, `ArtifactSystemdService = "systemd_service"`, `ArtifactRunnerFiles = "runner_files"`, and `ArtifactLocalState = "local_state"`. Define `CleanupArtifactPlan` with fields `Artifact CleanupArtifact json:"artifact"`, `Description string json:"description"`, `Action string json:"action"`, `DefaultSelected bool json:"default_selected"`, `RequiresConfirmation bool json:"requires_confirmation"`, `Blocked bool json:"blocked"`, and `BlockReason string json:"block_reason,omitempty"`; and `CleanupPlan` with fields `Repo string`, `RunnerName string`, `MachineTarget string`, `DryRun bool`, `Artifacts []CleanupArtifactPlan`, and `Warnings []string`. Implement `BuildCleanupPlan(repoState state.RepositoryState, dryRun bool) CleanupPlan` that produces exact action strings: `delete GitHub runner id 123 (runnerkit-owner-repo-local)`, `unconfigure runner registration from /opt/actions-runner/runnerkit-owner-repo-local`, `stop and uninstall service actions.runner.runnerkit-owner-repo-local.service`, `remove /opt/actions-runner/runnerkit-owner-repo-local and /var/lib/runnerkit/work/runnerkit-owner-repo-local`, and `remove local RunnerKit state for owner/repo after selected cleanup succeeds`. Implement `SafeRunnerPaths(repoState)` returning install path and work dir only when install path equals `/opt/actions-runner/<runner_name>` or has base `<runner_name>` under `/opt/actions-runner`, and work dir equals `/var/lib/runnerkit/work/<runner_name>`; it must return blocked when any candidate is `/var/lib/runnerkit`, `/var/lib`, `/opt`, `/opt/actions-runner`, `/`, empty, or does not end in the runner name. Add warning exactly `RunnerKit will not remove shared /var/lib/runnerkit or shared users.`. Tests must seed existing Phase 2 `Cleanup.ManagedPaths` containing `/var/lib/runnerkit` and assert that path is not included as a removal target.</action>
  <verify>
    <automated>go test ./internal/ops ./internal/state ./...</automated>
  </verify>
  <acceptance_criteria>
    - `internal/ops/cleanup.go` contains artifact strings `github_runner`, `host_registration`, `systemd_service`, `runner_files`, and `local_state`.
    - `internal/ops/cleanup.go` contains `BuildCleanupPlan` and `SafeRunnerPaths`.
    - `internal/ops/cleanup.go` contains `RunnerKit will not remove shared /var/lib/runnerkit or shared users.`.
    - `internal/ops/cleanup_test.go` asserts `/var/lib/runnerkit` is not a removal target.
    - `internal/ops/cleanup_test.go` asserts unsafe paths `/`, `/opt`, `/opt/actions-runner`, and `/var/lib/runnerkit` are blocked.
    - `go test ./internal/ops ./internal/state ./...` exits 0.</acceptance_criteria>
  <done>Cleanup has an explicit artifact plan and deletion safety guardrails before any command mutates GitHub or a host.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-04-02: Wire runnerkit down dry-run, JSON, and artifact-by-artifact confirmations</name>
  <files>internal/cli/root.go, internal/cli/down.go, internal/cli/down_test.go</files>
  <read_first>GEMINI.md, .planning/phases/03-operations-diagnostics-and-byo-cleanup/03-UI-SPEC.md, internal/cli/root.go, internal/cli/status.go, internal/cli/recover.go, internal/cli/down.go (if present), internal/cli/down_test.go (if present), internal/ops/cleanup.go, internal/state/store.go, internal/ui/prompt.go, internal/ui/output.go, internal/testsupport/github.go, internal/testsupport/remote.go, internal/testsupport/state.go</read_first>
  <behavior>
    - `runnerkit down --repo owner/repo --dry-run --no-color` prints `Step 1 of 1: cleanup plan` and performs no GitHub, remote, or state mutation.
    - Interactive `runnerkit down --repo owner/repo` asks each selected artifact prompt separately and defaults to No.
    - No-TTY mutation without `--yes` exits 6.
    - `--json --dry-run` emits the cleanup plan as JSON only.
  </behavior>
  <action>Add `newDownCommand(deps, jsonOutput, noColor)` to `internal/cli/down.go` and wire it from `internal/cli/root.go`. Define `downOptions` with exact fields `repo string`, `yes bool`, `dryRun bool`, `githubRunnerID int64`, and `runnerName string`; register flags `--repo`, `--yes`, `--dry-run`, `--github-runner-id`, and `--runner-name`. Implement repo targeting: `--repo` first, inferred git remote second, no prompts for inference; if neither exists, exit 6 with remediation `Pass --repo owner/name for BYO cleanup.`. Load local state with `Store.GetRepository`; if found, build `ops.BuildCleanupPlan`. Human plan output must start `Step 1 of 1: cleanup plan`, include `This will remove RunnerKit-managed runner artifacts for owner/repo.`, include artifact bullets for GitHub runner, host registration, service, files, and local state, and include exact next line `Next: answer each prompt, pass --dry-run to preview only, or pass --yes for the safe default plan.`. Implement prompt messages exactly: `Remove GitHub runner runnerkit-owner-repo-local from owner/repo? [y/N]`, `Unconfigure the runner registration on alice@example.com:22? [y/N]`, `Stop and uninstall service actions.runner.runnerkit-owner-repo-local.service on alice@example.com:22? [y/N]`, `Remove RunnerKit install path /opt/actions-runner/runnerkit-owner-repo-local and work dir /var/lib/runnerkit/work/runnerkit-owner-repo-local? [y/N]`, and `Remove local RunnerKit state for owner/repo? [y/N]`. Defaults are false. If `--yes`, select all safe default artifacts without prompts. If no TTY or JSON mode and not `--yes` or `--dry-run`, exit 6 with remediation `Pass --yes to apply the safe default cleanup plan non-interactively, or pass --dry-run to preview only.`. JSON dry-run must include keys `ok`, `command`, `repo`, `dry_run`, `plan`, `results`, `partial_cleanup`, `pending`, `state_removed`, and `redactions_applied` with `results: []`, `partial_cleanup: false`, and `state_removed: false`.</action>
  <verify>
    <automated>go test ./internal/cli ./internal/ops ./internal/state ./internal/ui ./...</automated>
  </verify>
  <acceptance_criteria>
    - `internal/cli/root.go` contains `newDownCommand`.
    - `internal/cli/down.go` contains `Use: "down"`, `github-runner-id`, `runner-name`, and `Step 1 of 1: cleanup plan`.
    - `internal/cli/down.go` contains all five exact prompt strings from the action.
    - `internal/cli/down_test.go` contains tests for `--dry-run`, `--json --dry-run`, no-TTY missing `--yes`, interactive prompts defaulting to No, and `--yes` no prompts.
    - `internal/cli/down_test.go` asserts `DeleteRunnerCalls` and remote command count remain `0` during dry-run.
    - `go test ./internal/cli ./internal/ops ./internal/state ./internal/ui ./...` exits 0.</acceptance_criteria>
  <done>`runnerkit down` previews cleanup scope safely and handles interactive/non-interactive confirmation correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-04-03: Execute host unregister, GitHub stale fallback, service uninstall, exact file removal, and complete state removal</name>
  <files>internal/cli/down.go, internal/cli/down_test.go, internal/bootstrap/script.go, internal/redact/redact_test.go</files>
  <read_first>.planning/phases/03-operations-diagnostics-and-byo-cleanup/03-RESEARCH.md, internal/cli/down.go, internal/cli/down_test.go, internal/cli/recover.go, internal/bootstrap/script.go, internal/github/runners.go, internal/github/tokens.go, internal/state/schema.go, internal/state/store.go, internal/remote/executor.go, internal/redact/redact.go, internal/testsupport/github.go, internal/testsupport/remote.go, internal/testsupport/state.go</read_first>
  <behavior>
    - Reachable BYO cleanup uses a fresh removal token for host-side `config.sh remove` before any GitHub API deletion, and redacts it.
    - GitHub API deletion is a fallback for stale records after host-side removal, SSH-unreachable cleanup, or local-state-missing explicit stale deletion; candidate selection uses saved `Cleanup.GitHubRunnerID` first, with safe fallback to saved runner name plus RunnerKit labels.
    - Service cleanup and file cleanup only target recorded service/install/work artifacts.
    - Complete selected cleanup removes the matching local state record.
  </behavior>
  <action>Implement down execution for selected artifacts with host-side deregistration preferred over GitHub API deletion. When local state has reachable SSH/install path and `host_registration` is selected, execute it before the `github_runner` API-delete fallback: call `deps.GitHub.CreateRemovalToken`, register `redact.RunnerRemovalToken`, and run `remote.Command{ID:"down.runner.remove", Script: bootstrap.RenderRemoveConfigScript(installPath, "runnerkit-runner"), Env: map[string]string{"RUNNERKIT_REMOVAL_TOKEN": token.Token}, RedactArgs: []string{token.Token}, Timeout:60*time.Second}`. Treat output containing `Not configured`, `does not exist`, or `already removed` as skipped/done. After host-side removal succeeds or is idempotently skipped, reconcile the `github_runner` artifact by calling `deps.GitHub.ListRunners`; select a leftover/stale runner ID from saved `Cleanup.GitHubRunnerID` when non-zero and present. If saved ID is not present, fallback only to a runner whose `Name` equals saved `Runner.Name` and whose labels include `runnerkit` plus the saved repo label `runnerkit-owner-repo`. If no candidate remains, record `skipped` with message `GitHub runner already absent`. If exactly one safe candidate remains, call `deps.GitHub.DeleteRunner(ctx, repo, id)` as the stale-record fallback and record result status `done`; if more than one candidate remains, fail with error code `github_runner_ambiguous` and do not remove local state. When `host_registration` is not selected or cannot run because SSH/local install is unavailable, `github_runner` may use the same API-delete fallback path described here and in Task 03-04-04. For `systemd_service`, run `remote.Command{ID:"down.service.uninstall", Script:"cd '<install_path>' && sudo ./svc.sh stop || true\ncd '<install_path>' && sudo ./svc.sh uninstall || true\nsudo systemctl disable --now '<service_name>' || true", Timeout:60*time.Second}`. For `runner_files`, call `ops.SafeRunnerPaths`; if safe, run `remote.Command{ID:"down.files.remove", Script:"sudo rm -rf -- '<install_path>' '<work_dir>'", Timeout:60*time.Second}`. Never include `/var/lib/runnerkit` by itself in this script. For `local_state`, remove the repository only after selected prior artifacts are `done` or `skipped`; call `Store.RemoveRepository(repo.FullName)`. JSON success must set `partial_cleanup:false` and `state_removed:true`. Tests must assert command ID order `down.runner.remove`, `down.service.uninstall`, `down.files.remove`, removal token redaction, host-side removal is attempted before any `DeleteRunner` call in reachable cleanup, stale fallback can delete runner ID `123`, and the state file no longer contains `owner/repo` after complete `--yes` cleanup.</action>
  <verify>
    <automated>go test ./internal/cli ./internal/bootstrap ./internal/state ./internal/github ./internal/remote ./internal/workflow ./internal/redact ./...</automated>
  </verify>
  <acceptance_criteria>
    - `internal/cli/down.go` contains command IDs `down.runner.remove`, `down.service.uninstall`, and `down.files.remove`.
    - `internal/cli/down.go` contains `CreateRemovalToken` and `DeleteRunner`.
    - `internal/cli/down.go` contains `sudo rm -rf --` and does not construct a removal script containing `/var/lib/runnerkit'` as a standalone target.
    - `internal/cli/down_test.go` contains a complete `--yes` cleanup test asserting `down.runner.remove` runs before any `DeleteRunner` call.
    - `internal/cli/down_test.go` contains a stale GitHub fallback test asserting deleted runner ID `123`.
    - `internal/cli/down_test.go` asserts raw `removal-token` test value is absent from stdout, stderr, and state.
    - `internal/cli/down_test.go` asserts state no longer contains `owner/repo` after complete cleanup.
    - `go test ./internal/cli ./internal/bootstrap ./internal/state ./internal/github ./internal/remote ./internal/workflow ./internal/redact ./...` exits 0.</acceptance_criteria>
  <done>Reachable BYO cleanup unconfigures the runner host-side first, deletes any remaining stale GitHub record, removes only safe artifacts, and removes local state when complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-04-04: Handle stale GitHub-only deletion, SSH-unreachable partial cleanup, and checkpointed state</name>
  <files>internal/cli/down.go, internal/cli/down_test.go, internal/state/schema.go, internal/state/store.go, internal/state/state_test.go</files>
  <read_first>.planning/phases/03-operations-diagnostics-and-byo-cleanup/03-CONTEXT.md, .planning/phases/03-operations-diagnostics-and-byo-cleanup/03-RESEARCH.md, internal/cli/down.go, internal/cli/down_test.go, internal/ops/cleanup.go, internal/state/schema.go, internal/state/store.go, internal/state/state_test.go, internal/github/runners.go, internal/testsupport/github.go, internal/testsupport/remote.go, internal/testsupport/state.go</read_first>
  <behavior>
    - SSH-unreachable cleanup can still delete the stale GitHub runner record and keeps local state with remote cleanup pending.
    - Local-state-missing cleanup requires explicit `--github-runner-id` for non-interactive stale GitHub deletion.
    - Missing GitHub runner does not block local service/file cleanup.
    - Missing service/path is idempotent skip, not a hard failure.
    - GitHub auth/delete failure preserves state and records pending GitHub cleanup.
  </behavior>
  <action>Extend `down` execution with partial result handling. If local state exists and SSH probe/status says unreachable, allow `github_runner` artifact execution; mark `host_registration`, `systemd_service`, and `runner_files` as `pending` with message `remote_cleanup_pending`; append state `Cleanup.Notes` value `remote_cleanup_pending`; append `OperationCheckpoint{Command:"down", Artifact:"remote", Status:"pending", Message:"SSH unreachable during cleanup", UpdatedAt: deps.Clock()}`; call `Store.UpdateRepository` instead of `RemoveRepository`; print `Cleanup incomplete` in human output and set JSON `partial_cleanup:true`, `pending:["remote_cleanup_pending"]`, and `state_removed:false`. If GitHub deletion fails, append note `github_cleanup_pending`, checkpoint artifact `github_runner` status `pending`, and keep state. If local state is missing and `--github-runner-id 123 --repo owner/repo --yes` is supplied, list GitHub runners, require an exact runner with ID `123`, call `DeleteRunner`, and do not run remote or state removal. If local state is missing and no `--github-runner-id` is supplied in non-interactive/JSON mode, exit 6 with remediation `Pass --github-runner-id <id> to delete a stale GitHub runner when local state is missing.`. If `--runner-name` is supplied without state, permit deletion only when exactly one listed runner has that name and labels include `runnerkit`; otherwise block with `github_runner_ambiguous`. Treat remote command errors for already missing service/path as skipped when stdout/stderr contains `No such file`, `not found`, `not loaded`, `not configured`, or `does not exist`; otherwise mark failed and keep state. Add tests for SSH unreachable + GitHub deletion partial state, local-state-missing explicit ID deletion, missing GitHub runner plus local cleanup success, ambiguous name block, GitHub delete error pending state, and missing service/path idempotent skip.</action>
  <verify>
    <automated>go test ./internal/cli ./internal/state ./internal/github ./internal/remote ./internal/workflow ./...</automated>
  </verify>
  <acceptance_criteria>
    - `internal/cli/down.go` contains `remote_cleanup_pending`, `github_cleanup_pending`, and `Cleanup incomplete`.
    - `internal/cli/down.go` contains `Pass --github-runner-id <id> to delete a stale GitHub runner when local state is missing.`.
    - `internal/cli/down_test.go` contains tests for `SSH unreachable`, `local state missing`, `github_runner_ambiguous`, `remote_cleanup_pending`, and `github_cleanup_pending`.
    - `internal/state/state_test.go` asserts pending cleanup notes/checkpoints persist after `Store.UpdateRepository`.
    - `go test ./internal/cli ./internal/state ./internal/github ./internal/remote ./internal/workflow ./...` exits 0.</acceptance_criteria>
  <done>Cleanup is resumable and useful even when SSH, GitHub, or local state is partially missing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 03-04-05: Document BYO down cleanup, stale GitHub deletion, and safety boundaries</name>
  <files>README.md, docs/byo-quickstart.md, internal/cli/docs_test.go</files>
  <read_first>.planning/phases/03-operations-diagnostics-and-byo-cleanup/03-CONTEXT.md, .planning/phases/03-operations-diagnostics-and-byo-cleanup/03-UI-SPEC.md, README.md, docs/byo-quickstart.md, internal/cli/docs_test.go, internal/cli/down.go, internal/ops/cleanup.go</read_first>
  <behavior>
    - Docs use `down` for BYO cleanup and avoid `destroy` for BYO.
    - Docs show dry-run before destructive cleanup.
    - Docs explicitly state what is removed and what is not removed.
    - Docs explain stale GitHub runner deletion when local state is missing.
  </behavior>
  <action>Update `README.md` with exact command strings `runnerkit down --repo owner/name --dry-run` and `runnerkit down --repo owner/name --yes`. Update `docs/byo-quickstart.md` with a new `## Cleanup` section containing exact command block lines `runnerkit down --repo owner/name --dry-run`, `runnerkit down --repo owner/name`, `runnerkit down --repo owner/name --yes`, and `runnerkit down --repo owner/name --github-runner-id 123 --yes`. Add exact safety sentences `RunnerKit down removes only RunnerKit-managed runner-specific BYO artifacts recorded in state.`, `RunnerKit down does not delete the BYO machine, shared users, shared /var/lib/runnerkit parents, or unrelated user data.`, and `Use destroy only for future cloud resources; BYO cleanup uses down.`. Add exact partial cleanup sentence `If SSH is unreachable, RunnerKit can delete the stale GitHub runner and keep local state with remote_cleanup_pending so you know what may remain on the host.`. Extend `internal/cli/docs_test.go` to assert those strings are present and that `runnerkit destroy --repo owner/name` is absent.</action>
  <verify>
    <automated>go test ./... && grep -R "runnerkit down --repo owner/name --dry-run" README.md docs/byo-quickstart.md</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains `runnerkit down --repo owner/name --dry-run`.
    - `docs/byo-quickstart.md` contains `runnerkit down --repo owner/name --github-runner-id 123 --yes`.
    - `docs/byo-quickstart.md` contains `RunnerKit down removes only RunnerKit-managed runner-specific BYO artifacts recorded in state.`.
    - `docs/byo-quickstart.md` contains `RunnerKit down does not delete the BYO machine, shared users, shared /var/lib/runnerkit parents, or unrelated user data.`.
    - `docs/byo-quickstart.md` contains `Use destroy only for future cloud resources; BYO cleanup uses down.`.
    - `docs/byo-quickstart.md` contains `remote_cleanup_pending`.
    - `internal/cli/docs_test.go` asserts `runnerkit destroy --repo owner/name` is absent.
    - `go test ./... && grep -R "runnerkit down --repo owner/name --dry-run" README.md docs/byo-quickstart.md` exits 0.</acceptance_criteria>
  <done>Users have explicit safe cleanup and stale-deregistration instructions for BYO managed runners.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go test ./...` exits 0.
- [ ] `go vet ./...` exits 0.
- [ ] `grep -R "Use: \"down\"" internal/cli/down.go` returns a match.
- [ ] `grep -R "remote_cleanup_pending" internal/cli internal/state docs` returns matches.
- [ ] `grep -R "RunnerKit down does not delete the BYO machine" docs/byo-quickstart.md` returns a match.
- [ ] `grep -R "runnerkit down --repo owner/name --github-runner-id 123 --yes" docs/byo-quickstart.md` returns a match.
- [ ] `go test ./internal/cli -run TestDown` exits 0.
</verification>

<success_criteria>

- GH-03 is covered because `runnerkit down` and recovery flows can deregister/delete GitHub Actions runner records when removing or recreating managed runners.
- CLEAN-02 is covered because stale GitHub runner records can be deleted even when SSH/local state is missing or partial, with explicit targeting and checkpoints.
- CLEAN-03 is covered because BYO cleanup removes only recorded RunnerKit-managed runner-specific service/files and avoids unrelated user data, shared users, and shared parent directories.
  </success_criteria>

<output>
After completion, create `.planning/phases/03-operations-diagnostics-and-byo-cleanup/03-04-SUMMARY.md`.
</output>
