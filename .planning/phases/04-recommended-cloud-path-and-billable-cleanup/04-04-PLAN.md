---
phase: 04-recommended-cloud-path-and-billable-cleanup
plan: "04"
type: execute
wave: 4
depends_on:
  - "04-03"
files_modified:
  - README.md
  - docs/cloud-quickstart.md
  - internal/cli/root.go
  - internal/cli/destroy.go
  - internal/cli/destroy_test.go
  - internal/cli/docs_test.go
  - internal/ops/cloud_destroy.go
  - internal/ops/cloud_destroy_test.go
  - internal/provider/provider.go
  - internal/provider/hetzner/client.go
  - internal/provider/hetzner/destroy.go
  - internal/provider/hetzner/destroy_test.go
  - internal/state/schema.go
  - internal/state/state_test.go
autonomous: true
requirements_addressed: [CLEAN-01, CLEAN-04, DOC-02, MACH-05]
requirements:
  - CLEAN-01
  - CLEAN-04
  - DOC-02
  - MACH-05
  - GH-03
user_setup: []
must_haves:
  truths:
    - "runnerkit destroy is the cloud billable cleanup command; runnerkit down remains BYO cleanup only."
    - "destroy --dry-run shows a plan before mutation with GitHub runner, remote cleanup, provider resources, local state, and billing impact."
    - "destroy --yes applies a safe default plan and does not remove local state until GitHub registration is absent and provider resources verify deleted or non-billable."
    - "Partial destroy keeps state with pending checkpoints and enough provider/GitHub/remote identity to resume."
    - "Cloud docs show provider auth, setup, labels, status/logs/doctor, destroy dry-run/apply, cost caveats, and Phase 4 limitations."
  artifacts:
    - path: "internal/cli/destroy.go"
      provides: "runnerkit destroy command with dry-run, --yes, typed interactive confirmation, JSON contract, partial cleanup handling"
      contains: 'Use: "destroy"'
    - path: "internal/ops/cloud_destroy.go"
      provides: "Cloud destroy plan, artifact ordering, partial checkpoint semantics, and non-billable verification model"
      contains: "BuildCloudDestroyPlan"
    - path: "internal/provider/hetzner/destroy.go"
      provides: "Hetzner server/SSH key/firewall/primary IP delete and verify-absent adapter methods"
      contains: "VerifyDestroyed"
    - path: "docs/cloud-quickstart.md"
      provides: "Concise cloud quickstart and cleanup verification guidance"
      contains: "runnerkit up --repo owner/name --cloud hetzner"
  key_links:
    - from: "internal/cli/root.go"
      to: "internal/cli/destroy.go"
      via: "root wires cloud destroy command"
      pattern: "newDestroyCommand"
    - from: "internal/cli/destroy.go"
      to: "internal/provider/provider.go"
      via: "destroy applies provider Destroy then VerifyDestroyed before state removal"
      pattern: "VerifyDestroyed"
    - from: "internal/cli/destroy.go"
      to: "internal/state/store.go"
      via: "state is removed only after GitHub and provider cleanup verify complete"
      pattern: "RemoveRepository"
---

<objective>
Add billable cloud cleanup and documentation for the recommended cloud path.

Purpose: The cloud path is only safe if users can see exactly what will be removed, destroy RunnerKit-created Hetzner resources, verify those resources are no longer billable, resume partial cleanup, and follow concise setup/destroy docs.

Output: `runnerkit destroy` command, cloud destroy planner, Hetzner delete/verify methods, partial cleanup checkpoints, JSON/human output with billing verification, README cloud section, and `docs/cloud-quickstart.md`.
</objective>

<execution_context>
@.pi/gsd/workflows/execute-plan.md
@.pi/gsd/templates/summary.md
</execution_context>

<context>
@GEMINI.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-recommended-cloud-path-and-billable-cleanup/04-CONTEXT.md
@.planning/phases/04-recommended-cloud-path-and-billable-cleanup/04-RESEARCH.md
@.planning/phases/04-recommended-cloud-path-and-billable-cleanup/04-VALIDATION.md
@.planning/phases/04-recommended-cloud-path-and-billable-cleanup/04-UI-SPEC.md
@.planning/phases/04-recommended-cloud-path-and-billable-cleanup/04-03-SUMMARY.md
@internal/cli/root.go
@internal/cli/down.go
@internal/cli/status.go
@internal/ops/cleanup.go
@internal/ops/status.go
@internal/provider/provider.go
@internal/provider/hetzner/provision.go
@internal/state/schema.go
@internal/state/store.go
@internal/bootstrap/script.go
@README.md
@docs/byo-quickstart.md

<interfaces>
Command contract:

```text
runnerkit destroy --repo owner/name --dry-run
runnerkit destroy --repo owner/name
runnerkit destroy --repo owner/name --yes
```

Required destructive copy from UI-SPEC:

```text
Cloud destroy plan
This removes RunnerKit-created cloud resources that may still be billing. Local state is removed only after GitHub and provider cleanup are verified.
Destroy cloud runner: type `destroy owner/name` to remove the GitHub runner registration and RunnerKit-created Hetzner resources.
Cleanup incomplete. RunnerKit kept state with pending cleanup checkpoints so you can rerun `runnerkit destroy --repo owner/name`.
Cloud runner destroyed. GitHub runner is absent and RunnerKit-created Hetzner resources are deleted or verified non-billable.
```

Cloud destroy artifacts:

```text
github_runner
remote_runner
provider_server
provider_ssh_key
provider_firewall
provider_primary_ip
local_state
```

JSON top-level keys:

```text
ok, command, repo, dry_run, plan, results, provider_verification, partial_cleanup, pending, state_removed, redactions_applied
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 04-04-01: Add runnerkit destroy dry-run plan, confirmation UX, and JSON contract</name>
  <files>internal/cli/root.go, internal/cli/destroy.go, internal/cli/destroy_test.go, internal/ops/cloud_destroy.go, internal/ops/cloud_destroy_test.go</files>
  <read_first>.planning/phases/04-recommended-cloud-path-and-billable-cleanup/04-CONTEXT.md, .planning/phases/04-recommended-cloud-path-and-billable-cleanup/04-UI-SPEC.md, internal/cli/root.go, internal/cli/down.go, internal/cli/down_test.go, internal/ops/cleanup.go, internal/state/schema.go, internal/state/store.go, internal/ui/prompt.go, internal/ui/output.go, internal/testsupport/state.go</read_first>
  <behavior>
    - `runnerkit destroy --repo owner/name --dry-run` renders a cloud destroy plan and mutates nothing.
    - No-TTY or JSON mutation without `--yes` fails with input-required remediation.
    - Interactive destroy requires typed confirmation `destroy owner/name`; default confirmation is not enough.
    - BYO states are rejected with guidance to use `runnerkit down`.
  </behavior>
  <action>Create `internal/ops/cloud_destroy.go` with `CloudDestroyArtifact` constants `ArtifactCloudGitHubRunner = "github_runner"`, `ArtifactCloudRemoteRunner = "remote_runner"`, `ArtifactCloudProviderServer = "provider_server"`, `ArtifactCloudProviderSSHKey = "provider_ssh_key"`, `ArtifactCloudProviderFirewall = "provider_firewall"`, `ArtifactCloudProviderPrimaryIP = "provider_primary_ip"`, and `ArtifactCloudLocalState = "local_state"`. Define `CloudDestroyArtifactPlan` with JSON fields `artifact`, `description`, `action`, `default_selected`, `requires_confirmation`, `blocked`, and `block_reason`; define `CloudDestroyPlan` with fields `repo`, `runner_name`, `provider`, `machine_target`, `dry_run`, `artifacts`, and `warnings`. Implement `BuildCloudDestroyPlan(repoState state.RepositoryState, dryRun bool) CloudDestroyPlan` that emits artifacts in this exact order: GitHub runner removal, remote runner/service cleanup, Hetzner server deletion, RunnerKit-created SSH key deletion, RunnerKit-created firewall deletion, Primary IPv4/IPv6 deletion or non-billable verification, local state removal. The warnings must include exactly `This removes RunnerKit-created cloud resources that may still be billing. Local state is removed only after GitHub and provider cleanup are verified.`. Create `internal/cli/destroy.go` with `destroyOptions{repo string, yes bool, dryRun bool}` and flags `--repo`, `--yes`, and `--dry-run`; wire `newDestroyCommand` in `internal/cli/root.go`. `runDestroy` must load state by repo, require `Provider.Kind` or `Provider.Name` equal `hetzner`, and reject BYO state with error remediation `Use runnerkit down --repo owner/name for BYO cleanup.`. Human dry-run must start `Step 1 of 1: Cloud destroy plan`, list each artifact action, include `Future billing stops only after provider resources are deleted or verified non-billable.`, and end with next action `Next: run runnerkit destroy --repo owner/name --yes after reviewing the plan.`. JSON dry-run must include `ok:true`, `command:"destroy"`, `repo`, `dry_run:true`, `plan`, `results:[]`, `provider_verification:null` or `provider_verification:{"status":"not_run"}`, `partial_cleanup:false`, `pending:[]`, `state_removed:false`, and `redactions_applied:true`. For interactive mutation, require typed input exactly `destroy owner/name` with prompt text exactly `Destroy cloud runner: type `destroy owner/name` to remove the GitHub runner registration and RunnerKit-created Hetzner resources.`; if the renderer/prompt implementation cannot preserve Markdown backticks in terminal prompts, tests must assert the literal prompt source constant includes the backticks and the rendered terminal output contains the same words without using backticks as the only signal. If no TTY/JSON and not `--yes` or `--dry-run`, exit input-required with remediation `Pass --yes to apply the cloud destroy plan non-interactively, or pass --dry-run to preview only.`. Add tests asserting dry-run has zero GitHub/provider/remote/state mutations, BYO state rejection, no-TTY missing `--yes`, interactive typed confirmation, and JSON key contract.</action>
  <verify>
    <automated>go test ./internal/cli/... ./internal/ops/... ./internal/provider/...</automated>
  </verify>
  <acceptance_criteria>
    - `internal/cli/root.go` contains `newDestroyCommand`.
    - `internal/cli/destroy.go` contains `Use: "destroy"`, `Cloud destroy plan`, and `destroy owner/name`.
    - `internal/ops/cloud_destroy.go` contains artifact strings `github_runner`, `remote_runner`, `provider_server`, `provider_ssh_key`, `provider_firewall`, `provider_primary_ip`, and `local_state`.
    - `internal/cli/destroy_test.go` asserts `destroy --dry-run` mutates no GitHub, provider, remote, or state dependencies and includes a `provider_verification` key with null/not-run value in JSON.
    - `internal/cli/destroy_test.go` asserts BYO states print `Use runnerkit down --repo owner/name for BYO cleanup.`.
    - `go test ./internal/cli/... ./internal/ops/... ./internal/provider/...` exits 0.</acceptance_criteria>
  <done>Users can review a complete billable-resource destroy plan before any cloud cleanup mutation happens.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 04-04-02: Apply cloud destroy, verify non-billable resources, and preserve partial cleanup state</name>
  <files>internal/cli/destroy.go, internal/cli/destroy_test.go, internal/ops/cloud_destroy.go, internal/ops/cloud_destroy_test.go, internal/provider/provider.go, internal/provider/hetzner/client.go, internal/provider/hetzner/destroy.go, internal/provider/hetzner/destroy_test.go, internal/state/schema.go, internal/state/state_test.go</files>
  <read_first>.planning/phases/04-recommended-cloud-path-and-billable-cleanup/04-RESEARCH.md, .planning/phases/04-recommended-cloud-path-and-billable-cleanup/04-VALIDATION.md, internal/cli/destroy.go, internal/cli/down.go, internal/ops/cloud_destroy.go, internal/ops/cleanup.go, internal/provider/provider.go, internal/provider/hetzner/provision.go, internal/state/schema.go, internal/state/store.go, internal/bootstrap/script.go, internal/github/runners.go, internal/testsupport/github.go, internal/testsupport/remote.go</read_first>
  <behavior>
    - Destroy removes or verifies absent the GitHub runner, remote registration/service/files when reachable, server, SSH key, firewall, and primary IPs tracked in state.
    - Destroy does not declare success until provider verification proves RunnerKit-created billable resources are absent or non-billable.
    - Partial failures keep state with pending checkpoints and do not hide billable resource IDs.
    - Complete destroy removes local state only after GitHub and provider verification pass.
  </behavior>
  <action>Implement provider destroy support. In `internal/provider/hetzner/client.go`, add wrapper methods for primary IP lifecycle: `GetPrimaryIP`, `DeletePrimaryIP`, and any typed 404/already-absent detection needed by tests. In `internal/provider/hetzner/destroy.go`, implement `Destroy(ctx, ref state.ProviderRef) (provider.DestroyResult, error)` and `VerifyDestroyed(ctx, ref state.ProviderRef) (provider.VerificationResult, error)` using the hcloud client wrapper from Plan 04-02 plus the new primary IP methods. `Destroy` must attempt resource cleanup in safe order: delete server ID first, delete RunnerKit-created SSH key ID, delete RunnerKit-created firewall ID, delete primary IPv4/IPv6 IDs if tracked separately and still billable, and tolerate already-absent 404s as skipped. `VerifyDestroyed` must re-describe every saved provider resource ID and return `OK:true` only if server/SSH key/firewall/primary IPs are absent or explicitly non-billable; a delete API response alone is not enough. In `internal/cli/destroy.go`, implement apply flow: collect status, if SSH reachable run host-side `config.sh remove` with fresh GitHub removal token and existing redaction pattern from `down`; delete the GitHub runner by saved `Cleanup.GitHubRunnerID` or safe runner name+labels fallback; call provider `Destroy`; call provider `VerifyDestroyed`; remove local state only if GitHub is absent/deleted, remote cleanup is done/skipped or SSH was intentionally unavailable, and provider verification `OK:true`. JSON complete success must set `ok:true`, `partial_cleanup:false`, `pending:[]`, `state_removed:true`, and include `provider_verification.ok:true`. Human complete success must include exact copy `Cloud runner destroyed. GitHub runner is absent and RunnerKit-created Hetzner resources are deleted or verified non-billable.`. If any step fails or verification returns billable resources, set `partial_cleanup:true`, keep local state, append `OperationCheckpoint{Command:"destroy", Artifact:<artifact>, Status:"pending", Message:<pending id>, UpdatedAt:deps.Clock()}`, preserve `Cleanup.ProviderResourceIDs`, and render exact copy `Cleanup incomplete. RunnerKit kept state with pending cleanup checkpoints so you can rerun runnerkit destroy --repo owner/name.`. Pending IDs must include one or more of `github_cleanup_pending`, `remote_cleanup_pending`, `provider_server_pending`, `provider_ssh_key_pending`, `provider_firewall_pending`, `provider_primary_ip_pending`, and `provider_verification_pending`. Add tests for complete destroy removes state, provider verify failure keeps state with resource IDs, GitHub delete failure keeps state, SSH unreachable still deletes GitHub/provider but leaves remote pending, already-absent provider resources are skipped and success, and raw removal/provider token strings are absent from stdout/stderr/state.</action>
  <verify>
    <automated>go test ./internal/cli/... ./internal/ops/... ./internal/provider/... ./internal/state/...</automated>
  </verify>
  <acceptance_criteria>
    - `internal/provider/hetzner/client.go` contains `GetPrimaryIP` and `DeletePrimaryIP`.
    - `internal/provider/hetzner/destroy.go` contains `VerifyDestroyed`, `DeleteServer`, `DeleteSSHKey`, `DeleteFirewall`, `DeletePrimaryIP`, and `primary`.
    - `internal/cli/destroy.go` contains `provider_verification_pending`, `provider_primary_ip_pending`, `Cleanup incomplete`, and `Cloud runner destroyed.`.
    - `internal/cli/destroy_test.go` asserts complete destroy output has `"state_removed":true` and state no longer contains `owner/repo`.
    - `internal/cli/destroy_test.go` asserts provider verification failure output has `"partial_cleanup":true`, `provider_verification_pending`, and keeps `server:<id>` in state.
    - `internal/provider/hetzner/destroy_test.go` asserts server, SSH key, firewall, and primary IP delete responses are followed by describe/verify calls before success.
    - `internal/cli/destroy_test.go` asserts raw removal token and fake provider token values are absent from stdout, stderr, and state.
    - `go test ./internal/cli/... ./internal/ops/... ./internal/provider/... ./internal/state/...` exits 0.</acceptance_criteria>
  <done>Cloud destroy is resumable, billing-safe, and refuses to remove local state while provider resources may still be billable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 04-04-03: Document cloud quickstart, cost caveats, lifecycle reuse, labels, and destroy verification</name>
  <files>README.md, docs/cloud-quickstart.md, internal/cli/docs_test.go</files>
  <read_first>.planning/phases/04-recommended-cloud-path-and-billable-cleanup/04-CONTEXT.md, .planning/phases/04-recommended-cloud-path-and-billable-cleanup/04-UI-SPEC.md, README.md, docs/byo-quickstart.md, internal/cli/docs_test.go</read_first>
  <behavior>
    - Docs get a concise happy-path cloud quickstart next to the BYO quickstart.
    - Docs include exact required command examples, workflow snippet, cost caveat, lifecycle commands, destroy commands, and Phase 4 limitations.
    - README includes the same required cloud command examples, workflow snippet, cost caveat, and Phase 4 limitations as the cloud quickstart, without implying RunnerKit edits workflow YAML or supports multiple providers in Phase 4.
  </behavior>
  <action>Create `docs/cloud-quickstart.md` with sections exactly `# Recommended Cloud Runner Quickstart`, `## Prerequisites`, `## Cost and billing caveat`, `## Run setup`, `## Add the workflow labels`, `## Check status and logs`, `## Destroy and verify cleanup`, `## Limitations`, and `## Optional live smoke test`. Include exact command block lines required by UI-SPEC: `export HCLOUD_TOKEN=...`, `runnerkit up --repo owner/name --cloud hetzner`, `runnerkit up --repo owner/name --cloud hetzner --yes`, `runnerkit status --repo owner/name`, `runnerkit logs --repo owner/name --since 30m --lines 200`, `runnerkit doctor --repo owner/name`, `runnerkit destroy --repo owner/name --dry-run`, `runnerkit destroy --repo owner/name`, and `runnerkit destroy --repo owner/name --yes`. Include CTA copy `Provision cloud runner`. Include exact workflow snippet `runs-on: [self-hosted, runnerkit, runnerkit-owner-repo, linux, x64, persistent]`. Include exact limitation sentences: `RunnerKit supports one recommended cloud path in Phase 4.`, `The default cloud runner is persistent and intended for trusted private repositories.`, `Ephemeral mode is deferred to Phase 5.`, `RunnerKit prints labels/snippets and does not edit workflow YAML.`, and `Cost estimates are approximate and billing stops only after relevant provider resources are destroyed or verified non-billable.`. Include provider auth setup using `HCLOUD_TOKEN`, mention `HETZNER_CLOUD_TOKEN` as accepted alias, explain that RunnerKit does not persist provider API tokens, and explain that live smoke tests require real Hetzner credentials and create billable resources. Update `README.md` with a cloud quickstart section linking to `docs/cloud-quickstart.md` and containing the exact same required command block lines, the exact workflow snippet, CTA copy `Provision cloud runner`, and all five exact limitation/cost sentences above. Extend `internal/cli/docs_test.go` to assert every command, the workflow snippet, CTA copy, and exact limitation/cost sentence appears in both README and `docs/cloud-quickstart.md`, and that README still includes BYO quickstart guidance.</action>
  <verify>
    <automated>go test ./... && grep -R "runnerkit up --repo owner/name --cloud hetzner" README.md docs/cloud-quickstart.md && grep -R "runnerkit destroy --repo owner/name" README.md docs/cloud-quickstart.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/cloud-quickstart.md` exists and contains `# Recommended Cloud Runner Quickstart`.
    - `docs/cloud-quickstart.md` contains `export HCLOUD_TOKEN=...`, `runnerkit up --repo owner/name --cloud hetzner`, `runnerkit up --repo owner/name --cloud hetzner --yes`, `runnerkit status --repo owner/name`, `runnerkit logs --repo owner/name --since 30m --lines 200`, `runnerkit doctor --repo owner/name`, `runnerkit destroy --repo owner/name --dry-run`, `runnerkit destroy --repo owner/name`, and `runnerkit destroy --repo owner/name --yes`.
    - `docs/cloud-quickstart.md` contains `runs-on: [self-hosted, runnerkit, runnerkit-owner-repo, linux, x64, persistent]`.
    - `docs/cloud-quickstart.md` contains all five exact Phase 4 limitation/cost sentences from the action.
    - `README.md` contains `docs/cloud-quickstart.md`, `Provision cloud runner`, `export HCLOUD_TOKEN=...`, `runnerkit up --repo owner/name --cloud hetzner`, `runnerkit up --repo owner/name --cloud hetzner --yes`, `runnerkit status --repo owner/name`, `runnerkit logs --repo owner/name --since 30m --lines 200`, `runnerkit doctor --repo owner/name`, `runnerkit destroy --repo owner/name --dry-run`, `runnerkit destroy --repo owner/name`, `runnerkit destroy --repo owner/name --yes`, and `runs-on: [self-hosted, runnerkit, runnerkit-owner-repo, linux, x64, persistent]`.
    - `README.md` contains all five exact Phase 4 limitation/cost sentences from the action.
    - `internal/cli/docs_test.go` asserts the required cloud docs command, workflow snippet, CTA, and limitation strings appear in both README and `docs/cloud-quickstart.md`.
    - `go test ./... && grep -R "runnerkit up --repo owner/name --cloud hetzner" README.md docs/cloud-quickstart.md && grep -R "runnerkit destroy --repo owner/name" README.md docs/cloud-quickstart.md` exits 0.</acceptance_criteria>
  <done>Developers can follow a concise cloud path from provider auth to setup, labels, operations, and verified billable cleanup.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go test ./internal/cli/... ./internal/ops/... ./internal/provider/... ./internal/state/...` exits 0.
- [ ] `go test ./...` exits 0.
- [ ] `grep -R "Use: \"destroy\"" internal/cli/destroy.go` returns a match.
- [ ] `grep -R "VerifyDestroyed" internal/provider internal/cli` returns matches.
- [ ] `grep -R "provider_verification_pending" internal/cli internal/ops internal/provider` returns matches.
- [ ] `grep -R "runnerkit up --repo owner/name --cloud hetzner" README.md docs/cloud-quickstart.md` returns matches.
- [ ] `grep -R "runnerkit destroy --repo owner/name" README.md docs/cloud-quickstart.md` returns matches.
</verification>

<success_criteria>

- CLEAN-01 is covered because `runnerkit destroy` always shows a plan before cloud cleanup mutation and `destroy --dry-run` exposes the plan without side effects.
- CLEAN-04 is covered because RunnerKit destroys RunnerKit-created Hetzner resources and verifies they are deleted or non-billable before removing local state.
- DOC-02 is covered because README and `docs/cloud-quickstart.md` document the recommended cloud path, status/logs/doctor lifecycle, labels, limitations, and destroy verification.
- MACH-05 remains covered because destroy/status uses the provider identity saved in state and preserves it during partial failures.
  </success_criteria>

<output>
After completion, create `.planning/phases/04-recommended-cloud-path-and-billable-cleanup/04-04-SUMMARY.md`.
</output>
