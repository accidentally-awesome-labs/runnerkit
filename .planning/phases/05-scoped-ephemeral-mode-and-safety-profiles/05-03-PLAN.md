---
phase: 05-scoped-ephemeral-mode-and-safety-profiles
plan: "03"
type: execute
wave: 3
depends_on:
  - "05-02"
files_modified:
  - README.md
  - docs/safety.md
  - docs/byo-quickstart.md
  - docs/cloud-quickstart.md
  - internal/cli/docs_test.go
  - internal/cli/up_ephemeral_e2e_test.go
  - internal/cli/status_test.go
  - internal/cli/logs_test.go
  - internal/cli/doctor_test.go
  - internal/cli/down_test.go
  - internal/cli/destroy_test.go
  - internal/testsupport/state.go
autonomous: true
requirements_addressed: [DOC-03, RUN-04, RUN-02]
requirements:
  - DOC-03
  - RUN-04
  - RUN-02
user_setup: []
must_haves:
  truths:
    - "docs/safety.md exists with the exact Self-hosted Runner Safety Guide headings and required sentences."
    - "README, BYO quickstart, and cloud quickstart no longer say ephemeral mode is deferred; they link to safety guidance and include ephemeral cloud commands."
    - "Automated tests prove trusted private persistent remains valid, risky persistent blocks, ephemeral cloud is recommended/allowed, and BYO ephemeral caveats are visible."
    - "End-to-end fake tests cover TTL/log-preservation/status/cleanup semantics for both trusted and untrusted workflow scenarios without live GitHub or Hetzner calls."
    - "Full regression and docs greps validate RunnerKit does not imply workflow YAML edits, autoscaling, ARC, JIT, or a fleet manager."
  artifacts:
    - path: "docs/safety.md"
      provides: "Safety guide explaining persistent risk, ephemeral recommendations, BYO/cloud caveats, logs, cleanup, and v1 non-goals"
      contains: "# Self-hosted Runner Safety Guide"
    - path: "README.md"
      provides: "Top-level link and commands for ephemeral cloud setup and safety guidance"
      contains: "runnerkit up --repo owner/name --mode ephemeral --cloud hetzner"
    - path: "internal/cli/up_ephemeral_e2e_test.go"
      provides: "Fake end-to-end CLI coverage for trusted/private and risky/untrusted mode choices"
      contains: "TestEphemeralCloudRecommendedForPublicRepo"
    - path: "internal/cli/docs_test.go"
      provides: "Docs command/sentence/heading assertions for safety guide and quickstarts"
      contains: "Self-hosted Runner Safety Guide"
  key_links:
    - from: "docs/safety.md"
      to: "internal/cli/docs_test.go"
      via: "docs tests assert exact safety headings, command examples, and required caveat sentences"
      pattern: "Persistent vs ephemeral tradeoffs"
    - from: "internal/cli/up_ephemeral_e2e_test.go"
      to: "internal/cli/up.go"
      via: "fake CLI runs validate mode selection, side-effect timing, lifecycle output, and safety copy"
      pattern: "--mode ephemeral"
    - from: "internal/cli/destroy_test.go"
      to: "internal/cli/logs.go"
      via: "cleanup tests verify preserved ephemeral logs before resource deletion"
      pattern: "ephemeral.logs.preserve"
---

<objective>
Document self-hosted runner safety guidance and add end-to-end validation for trusted and untrusted persistent/ephemeral scenarios.

Purpose: Phase 5 is complete only when developers can read when persistent runners are unsafe, when ephemeral mode is recommended, what cleanup/log duties remain, and when tests prove RunnerKit's UX and lifecycle do not overpromise autoscaling or clean-VM guarantees.

Output: `docs/safety.md`, README/BYO/cloud quickstart updates, docs assertions, fake E2E tests for persistent and ephemeral safety profiles, and final regression coverage for risky workload, TTL, log preservation, cleanup, and non-goal copy.
</objective>

<execution_context>
@.pi/gsd/workflows/execute-plan.md
@.pi/gsd/templates/summary.md
</execution_context>

<context>
@GEMINI.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-scoped-ephemeral-mode-and-safety-profiles/05-RESEARCH.md
@.planning/phases/05-scoped-ephemeral-mode-and-safety-profiles/05-VALIDATION.md
@.planning/phases/05-scoped-ephemeral-mode-and-safety-profiles/05-UI-SPEC.md
@.planning/phases/05-scoped-ephemeral-mode-and-safety-profiles/05-01-SUMMARY.md
@.planning/phases/05-scoped-ephemeral-mode-and-safety-profiles/05-02-SUMMARY.md
@README.md
@docs/byo-quickstart.md
@docs/cloud-quickstart.md
@internal/cli/docs_test.go
@internal/cli/up_test.go
@internal/cli/up_cloud_test.go
@internal/cli/up_ephemeral_test.go
@internal/cli/status_test.go
@internal/cli/logs_test.go
@internal/cli/doctor_test.go
@internal/cli/down_test.go
@internal/cli/destroy_test.go
@internal/testsupport/state.go

<interfaces>
Required docs files:

```text
README.md
docs/safety.md
docs/cloud-quickstart.md
docs/byo-quickstart.md
```

Required `docs/safety.md` headings:

```markdown
# Self-hosted Runner Safety Guide

## Quick recommendation

## Persistent vs ephemeral tradeoffs

## When persistent is appropriate

## When ephemeral is recommended

## Public and fork-based workflow risk

## BYO ephemeral caveats

## Cloud ephemeral caveats

## Logs and troubleshooting

## Cleanup commands

## What RunnerKit does not do in v1
```

Required docs command examples:

```bash
runnerkit up --repo owner/name --mode persistent --host user@host
runnerkit up --repo owner/name --mode ephemeral --host user@host
runnerkit up --repo owner/name --mode ephemeral --cloud hetzner
runnerkit up --repo owner/name --mode ephemeral --cloud hetzner --yes
runnerkit up --repo owner/name --mode ephemeral --cloud hetzner --ephemeral-ttl 24h
runnerkit status --repo owner/name
runnerkit logs --repo owner/name --since 30m --lines 200
runnerkit doctor --repo owner/name
runnerkit down --repo owner/name --dry-run
runnerkit destroy --repo owner/name --dry-run
runnerkit destroy --repo owner/name --yes
```

Required docs sentences:

```text
Persistent self-hosted runners are unsafe for public, fork-based, or otherwise untrusted workflows.
Ephemeral mode gives stronger isolation by using one-job GitHub runner registration, but it is not a clean VM by itself.
Ephemeral mode is not a fleet manager. RunnerKit creates one scoped runner; jobs with matching labels can still queue if no runner is online.
BYO ephemeral mode is a one-job GitHub registration, not a clean virtual machine.
Ephemeral cloud runners still create billable Hetzner resources.
Billing stops only after `runnerkit destroy --repo owner/name` verifies cleanup.
Estimated cost is approximate. Hetzner pricing varies by region and time, and you are responsible for charges until `runnerkit destroy --repo owner/name` verifies cleanup.
RunnerKit preserves best-effort runner `_diag` and systemd journal logs before cleanup.
Configure external log forwarding for production-grade ephemeral troubleshooting.
RunnerKit prints labels/snippets and does not edit workflow YAML.
Do not use `runs-on: self-hosted` alone for RunnerKit-managed runners.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 05-03-01: Create safety guide and update README/BYO/cloud docs with exact commands and caveats</name>
  <files>README.md, docs/safety.md, docs/byo-quickstart.md, docs/cloud-quickstart.md, internal/cli/docs_test.go</files>
  <read_first>.planning/phases/05-scoped-ephemeral-mode-and-safety-profiles/05-UI-SPEC.md, .planning/phases/05-scoped-ephemeral-mode-and-safety-profiles/05-RESEARCH.md, README.md, docs/safety.md, docs/byo-quickstart.md, docs/cloud-quickstart.md, internal/cli/docs_test.go</read_first>
  <behavior>
    - Safety docs explain mode choice without implying ephemeral is absolute safety, a clean VM on BYO, or an autoscaling fleet.
    - README links to `docs/safety.md` and includes the exact ephemeral cloud command.
    - BYO quickstart recommends ephemeral cloud for risky workloads instead of saying to wait for future ephemeral mode.
    - Cloud quickstart includes ephemeral cloud setup, TTL, log preservation, billable cleanup, approximate/pricing-varies/user-responsible cost caveats, and persistent-vs-ephemeral tradeoffs.
  </behavior>
  <action>Create `docs/safety.md` with heading `# Self-hosted Runner Safety Guide` and these exact section headings: `## Quick recommendation`, `## Persistent vs ephemeral tradeoffs`, `## When persistent is appropriate`, `## When ephemeral is recommended`, `## Public and fork-based workflow risk`, `## BYO ephemeral caveats`, `## Cloud ephemeral caveats`, `## Logs and troubleshooting`, `## Cleanup commands`, and `## What RunnerKit does not do in v1`. In `## Quick recommendation`, include bullets with exact commands `runnerkit up --repo owner/name --mode persistent --host user@host`, `runnerkit up --repo owner/name --mode ephemeral --host user@host`, and `runnerkit up --repo owner/name --mode ephemeral --cloud hetzner`. In `## Persistent vs ephemeral tradeoffs`, include a markdown table with columns `Mode`, `Cost`, `Isolation`, `Cleanup`, `Operations`, and `Logs`; rows must include `persistent` and `ephemeral`. In the guide, include the lower-case validation phrase `persistent self-hosted runners` and these exact sentences: `Persistent self-hosted runners are unsafe for public, fork-based, or otherwise untrusted workflows.`, `Ephemeral mode gives stronger isolation by using one-job GitHub runner registration, but it is not a clean VM by itself.`, `Ephemeral mode is not a fleet manager. RunnerKit creates one scoped runner; jobs with matching labels can still queue if no runner is online.`, `BYO ephemeral mode is a one-job GitHub registration, not a clean virtual machine.`, `Ephemeral cloud runners still create billable Hetzner resources.`, `Billing stops only after `runnerkit destroy --repo owner/name` verifies cleanup.`, "Estimated cost is approximate. Hetzner pricing varies by region and time, and you are responsible for charges until `runnerkit destroy --repo owner/name` verifies cleanup.", `RunnerKit preserves best-effort runner `_diag` and systemd journal logs before cleanup.`, `Configure external log forwarding for production-grade ephemeral troubleshooting.`, `RunnerKit prints labels/snippets and does not edit workflow YAML.`, and `Do not use `runs-on: self-hosted` alone for RunnerKit-managed runners.`. Include these exact commands: `runnerkit up --repo owner/name --mode persistent --host user@host`, `runnerkit up --repo owner/name --mode ephemeral --host user@host`, `runnerkit up --repo owner/name --mode ephemeral --cloud hetzner`, `runnerkit up --repo owner/name --mode ephemeral --cloud hetzner --yes`, `runnerkit up --repo owner/name --mode ephemeral --cloud hetzner --ephemeral-ttl 24h`, `runnerkit status --repo owner/name`, `runnerkit logs --repo owner/name --since 30m --lines 200`, `runnerkit doctor --repo owner/name`, `runnerkit down --repo owner/name --dry-run`, `runnerkit destroy --repo owner/name --dry-run`, and `runnerkit destroy --repo owner/name --yes`. In `## What RunnerKit does not do in v1`, include exact bullets `No hosted control plane.`, `No webhook listener or autoscaling fleet manager.`, `No Actions Runner Controller, Kubernetes, runner scale sets, organization-level runner management, or JIT runner API.`, `No automatic workflow YAML edits.`, and `No guarantee that BYO ephemeral mode is a clean VM.`. Update `README.md` to link `[Self-hosted Runner Safety Guide](docs/safety.md)`, include the lower-case validation phrase `persistent self-hosted runners`, include `Use ephemeral cloud runner`, include "Estimated cost is approximate. Hetzner pricing varies by region and time, and you are responsible for charges until `runnerkit destroy --repo owner/name` verifies cleanup.", include `runnerkit up --repo owner/name --mode ephemeral --cloud hetzner`, include `runnerkit up --repo owner/name --mode ephemeral --cloud hetzner --yes`, include `runnerkit up --repo owner/name --mode ephemeral --cloud hetzner --ephemeral-ttl 24h`, and include both workflow snippets `runs-on: [self-hosted, runnerkit, runnerkit-owner-repo, linux, x64, persistent]` and `runs-on: [self-hosted, runnerkit, runnerkit-owner-repo, linux, x64, ephemeral]`. Update `docs/byo-quickstart.md` so the safety warning contains exact sentence `Persistent self-hosted runners are unsafe for public, fork-based, or otherwise untrusted workflows.` and recommendation `Use runnerkit up --repo owner/name --mode ephemeral --cloud hetzner for stronger isolation, or use GitHub-hosted runners.`; remove the sentence `wait for RunnerKit's future ephemeral mode`. Update `docs/cloud-quickstart.md` so it no longer contains `Ephemeral mode is deferred to Phase 5.`, includes the exact command examples from this plan, includes `Ephemeral cloud runners still create billable Hetzner resources.`, includes `Billing stops only after `runnerkit destroy --repo owner/name` verifies cleanup.`, and includes "Estimated cost is approximate. Hetzner pricing varies by region and time, and you are responsible for charges until `runnerkit destroy --repo owner/name` verifies cleanup.". Extend `internal/cli/docs_test.go` with `TestSafetyGuideDocsContainRequiredCopy` that reads README, `docs/safety.md`, `docs/byo-quickstart.md`, and `docs/cloud-quickstart.md`; assert every heading, command, required sentence, and non-goal bullet is present; assert README links `docs/safety.md`; assert no docs file contains `Ephemeral mode is deferred to Phase 5` or `wait for RunnerKit's future ephemeral mode`.</action>
  <verify>
    <automated>go test ./internal/cli/... && grep -R "runnerkit up --repo owner/name --mode ephemeral --cloud hetzner" README.md docs && grep -R "Self-hosted Runner Safety Guide" README.md docs/safety.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/safety.md` exists and contains `# Self-hosted Runner Safety Guide`.
    - `docs/safety.md` contains `persistent self-hosted runners` and all exact headings `## Quick recommendation`, `## Persistent vs ephemeral tradeoffs`, `## When persistent is appropriate`, `## When ephemeral is recommended`, `## Public and fork-based workflow risk`, `## BYO ephemeral caveats`, `## Cloud ephemeral caveats`, `## Logs and troubleshooting`, `## Cleanup commands`, and `## What RunnerKit does not do in v1`.
    - `README.md` contains `docs/safety.md`, `persistent self-hosted runners`, `Use ephemeral cloud runner`, "Estimated cost is approximate. Hetzner pricing varies by region and time, and you are responsible for charges until `runnerkit destroy --repo owner/name` verifies cleanup.", `runnerkit up --repo owner/name --mode ephemeral --cloud hetzner`, and `runs-on: [self-hosted, runnerkit, runnerkit-owner-repo, linux, x64, ephemeral]`.
    - `docs/byo-quickstart.md` contains `Persistent self-hosted runners are unsafe for public, fork-based, or otherwise untrusted workflows.` and does not contain `wait for RunnerKit's future ephemeral mode`.
    - `docs/cloud-quickstart.md` contains `runnerkit up --repo owner/name --mode ephemeral --cloud hetzner --ephemeral-ttl 24h`, "Estimated cost is approximate. Hetzner pricing varies by region and time, and you are responsible for charges until `runnerkit destroy --repo owner/name` verifies cleanup.", and does not contain `Ephemeral mode is deferred to Phase 5.`.
    - `internal/cli/docs_test.go` contains `TestSafetyGuideDocsContainRequiredCopy`, `Self-hosted Runner Safety Guide`, `Ephemeral mode is not a fleet manager`, "Estimated cost is approximate. Hetzner pricing varies by region and time, and you are responsible for charges until `runnerkit destroy --repo owner/name` verifies cleanup.", and `No automatic workflow YAML edits.`.
    - `go test ./internal/cli/... && grep -R "runnerkit up --repo owner/name --mode ephemeral --cloud hetzner" README.md docs && grep -R "Self-hosted Runner Safety Guide" README.md docs/safety.md` exits 0.</acceptance_criteria>
  <done>Developer-facing docs explain when persistent is unsafe, when ephemeral is recommended, how cleanup/logs work, and what RunnerKit v1 intentionally does not provide.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 05-03-02: Add fake end-to-end tests for trusted persistent and risky ephemeral workflow scenarios</name>
  <files>internal/cli/up_ephemeral_e2e_test.go, internal/cli/up_modes_test.go, internal/cli/up_cloud_test.go, internal/cli/up_byo_test.go, internal/testsupport/state.go</files>
  <read_first>.planning/phases/05-scoped-ephemeral-mode-and-safety-profiles/05-VALIDATION.md, .planning/phases/05-scoped-ephemeral-mode-and-safety-profiles/05-UI-SPEC.md, internal/cli/up_ephemeral_e2e_test.go, internal/cli/up_ephemeral_test.go, internal/cli/up_modes_test.go, internal/cli/up_cloud_test.go, internal/cli/up_byo_test.go, internal/testsupport/github.go, internal/testsupport/remote.go, internal/testsupport/state.go, internal/provider/fake.go, internal/runmode/mode.go</read_first>
  <behavior>
    - Tests exercise real Cobra command paths with fake GitHub, remote, provider, state, and clock dependencies.
    - Trusted private persistent setup remains the default and still uses the persistent service path.
    - Public/fork persistent setup blocks before side effects and recommends ephemeral cloud.
    - Public/fork ephemeral cloud setup reaches the ephemeral bootstrap path after cloud readiness and prints warnings without claiming absolute safety.
    - BYO ephemeral public/fork setup requires the explicit BYO caveat acknowledgement.
  </behavior>
  <action>Create `internal/cli/up_ephemeral_e2e_test.go`. Add helper `ephemeralE2EDeps(t, repo gh.Repo) (Dependencies, *testsupport.GitHubService, *testsupport.RemoteExecutor, *provider.FakeProvider, string)` that uses a fixed clock `2026-05-01T18:30:00Z`, fake GitHub repository metadata, a fake remote executor with host key `SHA256:fakehostfingerprint`, fake provider returning a ready Hetzner machine, temp state dir, and `noSleep`. Add `TestPersistentPrivateDefaultStillUsesPersistentProfile` running `runnerkit up --repo owner/name --host user@host --dry-run --yes --no-color`; assert output contains `Default mode: persistent for trusted private repositories.`, `Safety profile: persistent-trusted`, `runs-on: [self-hosted, runnerkit, runnerkit-owner-name, linux, x64, persistent]`, and does not contain `--ephemeral`; assert provider calls are `0`. Add `TestPublicPersistentBlocksAndRecommendsEphemeralCloud` running public repo `runnerkit up --repo owner/name --mode persistent --host user@host --yes --no-color`; assert exit code `ExitSafetyGate`, output contains `Persistent self-hosted runners are unsafe for public, fork-based, or otherwise untrusted workflows.` and `runnerkit up --repo owner/name --mode ephemeral --cloud hetzner`; assert `VerifyAuthCalls == 0`, `VerifyRunnerManagementReadCalls == 0`, `CreateRegistrationTokenCalls == 0`, remote command count `0`, provider `ValidateCalls == 0`, and no state file exists. Add `TestEphemeralCloudRecommendedForPublicRepo` running public repo `runnerkit up --repo owner/name --mode ephemeral --cloud hetzner --yes --no-color`; assert output contains `Use ephemeral cloud runner`, "Estimated cost is approximate. Hetzner pricing varies by region and time, and you are responsible for charges until `runnerkit destroy --repo owner/name` verifies cleanup.", `Ephemeral runner ready`, `GitHub will assign at most one job to this runner, then automatically deregister it.`, `Ephemeral mode is not a fleet manager. RunnerKit creates one scoped runner; jobs with matching labels can still queue if no runner is online.`, and `Cleanup after the job: runnerkit destroy --repo owner/name`; assert saved state has `Runner.Mode == "ephemeral"`, `Safety.SafetyProfile == "ephemeral-cloud"`, `Ephemeral.TTL == "24h"`, provider tag `mode=ephemeral`, and `CreateRegistrationTokenCalls == 1`. Add `TestEphemeralBYOPublicRequiresAcknowledgement` running public repo `runnerkit up --repo owner/name --mode ephemeral --host user@host --yes --no-color` without `--allow-ephemeral-byo-risk`; assert `ExitSafetyGate` and caveat copy. Add `TestEphemeralBYOTrustedPrivateUsesDownCleanup` running private repo `runnerkit up --repo owner/name --mode ephemeral --host user@host --yes --no-color`; assert saved state cleanup command `runnerkit down --repo owner/name`, mode `ephemeral`, and no provider provisioning calls. Update `internal/testsupport/state.go` with `EphemeralBYORepositoryState()` and `EphemeralCloudRepositoryState()` fixtures containing runner name `runnerkit-owner-repo-ephemeral-20260501t183000`, labels with `ephemeral`, `Ephemeral.LogArchivePath`, and cleanup commands. Ensure tests do not require live GitHub or Hetzner credentials.</action>
  <verify>
    <automated>go test ./internal/cli/... ./internal/bootstrap/... ./internal/provider/... ./internal/state/...</automated>
  </verify>
  <acceptance_criteria>
    - `internal/cli/up_ephemeral_e2e_test.go` contains `TestPersistentPrivateDefaultStillUsesPersistentProfile`, `TestPublicPersistentBlocksAndRecommendsEphemeralCloud`, `TestEphemeralCloudRecommendedForPublicRepo`, `TestEphemeralBYOPublicRequiresAcknowledgement`, and `TestEphemeralBYOTrustedPrivateUsesDownCleanup`.
    - `internal/cli/up_ephemeral_e2e_test.go` contains exact command/copy strings `runnerkit up --repo owner/name --mode ephemeral --cloud hetzner`, "Estimated cost is approximate. Hetzner pricing varies by region and time, and you are responsible for charges until `runnerkit destroy --repo owner/name` verifies cleanup.", `Ephemeral runner ready`, `Ephemeral mode is not a fleet manager`, and `Cleanup after the job: runnerkit destroy --repo owner/name`.
    - `internal/cli/up_ephemeral_e2e_test.go` asserts side-effect counts `VerifyAuthCalls == 0`, `VerifyRunnerManagementReadCalls == 0`, `CreateRegistrationTokenCalls == 0`, and `ValidateCalls == 0` for blocked public persistent setup.
    - `internal/testsupport/state.go` contains `EphemeralBYORepositoryState` and `EphemeralCloudRepositoryState`.
    - `internal/testsupport/state.go` contains `runnerkit-owner-repo-ephemeral-20260501t183000`, `"ephemeral"`, `/var/lib/runnerkit/ephemeral/`, `runnerkit down --repo owner/repo`, and `runnerkit destroy --repo owner/repo`.
    - `go test ./internal/cli/... ./internal/bootstrap/... ./internal/provider/... ./internal/state/...` exits 0.</acceptance_criteria>
  <done>High-level fake CLI tests validate the user-facing safety decisions for trusted persistent, risky persistent, ephemeral cloud, and BYO ephemeral scenarios.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 05-03-03: Add lifecycle regression tests for TTL, preserved logs, cleanup, docs grep, and full suite</name>
  <files>internal/cli/status_test.go, internal/cli/logs_test.go, internal/cli/doctor_test.go, internal/cli/down_test.go, internal/cli/destroy_test.go, internal/cli/docs_test.go, internal/testsupport/state.go</files>
  <read_first>.planning/phases/05-scoped-ephemeral-mode-and-safety-profiles/05-VALIDATION.md, .planning/phases/05-scoped-ephemeral-mode-and-safety-profiles/05-UI-SPEC.md, internal/cli/status_test.go, internal/cli/logs_test.go, internal/cli/doctor_test.go, internal/cli/down_test.go, internal/cli/destroy_test.go, internal/cli/docs_test.go, internal/testsupport/state.go, internal/ops/status.go, internal/ops/logs.go, internal/ops/doctor.go</read_first>
  <behavior>
    - Regression tests prove ephemeral status classification differs from persistent missing-runner recovery.
    - Logs tests prove preserved archives are surfaced with the external-forwarding caveat.
    - Cleanup tests prove BYO `down` and cloud `destroy` preserve logs before deleting runner files/cloud resources.
    - Docs grep tests protect required safety copy and non-goals through full regression.
  </behavior>
  <action>Extend `internal/cli/status_test.go` with `TestStatusEphemeralCompletedNeedsCleanup` using `testsupport.EphemeralCloudRepositoryState()` and an empty GitHub runner list; fake remote command `status.ephemeral.state` returns `{"finalizer_status":"completed"}`; assert human output contains `Ephemeral runner completed one job and needs cleanup.`, `Next: runnerkit destroy --repo owner/repo`, `Log archive: /var/lib/runnerkit/ephemeral/runnerkit-owner-repo-ephemeral-20260501t183000/logs`, and does not contain `runnerkit recover --repo`. Add `TestMissingStateRendersRunnerKitEmptyState` asserting "No RunnerKit-managed runner is saved for `owner/name`." and "Run `runnerkit up --repo owner/name --mode ephemeral --cloud hetzner` for a one-job cloud runner, or use `--host user@host` for an existing machine.". Add `TestStatusEphemeralTTLExpired` with clock after `ExpiresAt` and sentinel `{"finalizer_status":"ttl_expired"}`; assert output contains `Ephemeral runner TTL expired before a job completed. Run cleanup now.` and health reason `ephemeral_ttl_expired` in JSON. Extend `internal/cli/logs_test.go` with `TestLogsEphemeralPreservedArchive` where fake remote returns archive list paths `Runner_20260501.log`, `Worker_20260501.log`, and `systemd-journal.log`; assert output contains `RunnerKit preserves best-effort logs only; configure external log forwarding for production-grade ephemeral troubleshooting.`, `Runner_20260501.log`, `Worker_20260501.log`, and `systemd-journal.log`. Extend `internal/cli/doctor_test.go` with `TestDoctorEphemeralCompletedRecommendsCleanup` asserting finding/next action `runnerkit destroy --repo owner/repo` or `runnerkit down --repo owner/repo` based on fixture kind. Extend `internal/cli/down_test.go` with `TestDownEphemeralPreservesLogsBeforeRemovingFiles` asserting fake remote command IDs contain `ephemeral.logs.preserve` before `down.files.remove`. Extend `internal/cli/destroy_test.go` with `TestDestroyEphemeralCloudPreservesLogsBeforeProviderDelete` asserting command ID `ephemeral.logs.preserve` occurs before provider `DestroyCalls == 1`, output includes `provider_verification`, and confirmation prompt is exactly "Destroy ephemeral cloud runner: type `destroy owner/name` to remove the GitHub runner registration and RunnerKit-created Hetzner resources.". Extend `internal/cli/docs_test.go` with `TestSafetyDocsGrepContract` asserting README and docs contain `persistent self-hosted runners`, `Ephemeral mode is not a fleet manager`, "Estimated cost is approximate. Hetzner pricing varies by region and time, and you are responsible for charges until `runnerkit destroy --repo owner/name` verifies cleanup.", `Configure external log forwarding for production-grade ephemeral troubleshooting.`, and do not contain `autoscaling fleet manager` unless preceded by `No ` or `not a `. Ensure all tests are fake-only and do not require live credentials. Run final commands exactly from validation strategy: `go test ./...`, `grep -R "persistent self-hosted runners" README.md docs`, and `grep -R "Ephemeral mode is not a fleet manager" README.md docs`, and `grep -R "Estimated cost is approximate. Hetzner pricing varies by region and time" README.md docs`.</action>
  <verify>
    <automated>go test ./... && grep -R "persistent self-hosted runners" README.md docs && grep -R "Ephemeral mode is not a fleet manager" README.md docs && grep -R "Estimated cost is approximate. Hetzner pricing varies by region and time" README.md docs</automated>
  </verify>
  <acceptance_criteria>
    - `internal/cli/status_test.go` contains `TestStatusEphemeralCompletedNeedsCleanup`, `TestMissingStateRendersRunnerKitEmptyState`, `ephemeral_completed`, `Ephemeral runner completed one job and needs cleanup.`, and "No RunnerKit-managed runner is saved for `owner/name`.".
    - `internal/cli/status_test.go` contains `TestStatusEphemeralTTLExpired`, `ephemeral_ttl_expired`, and `Ephemeral runner TTL expired before a job completed. Run cleanup now.`.
    - `internal/cli/logs_test.go` contains `TestLogsEphemeralPreservedArchive`, `Runner_20260501.log`, `Worker_20260501.log`, and `systemd-journal.log`.
    - `internal/cli/down_test.go` contains `TestDownEphemeralPreservesLogsBeforeRemovingFiles` and `ephemeral.logs.preserve`.
    - `internal/cli/destroy_test.go` contains `TestDestroyEphemeralCloudPreservesLogsBeforeProviderDelete`, `ephemeral.logs.preserve`, `provider_verification`, and "Destroy ephemeral cloud runner: type `destroy owner/name` to remove the GitHub runner registration and RunnerKit-created Hetzner resources.".
    - `internal/cli/docs_test.go` contains `TestSafetyDocsGrepContract`, `persistent self-hosted runners`, "Estimated cost is approximate. Hetzner pricing varies by region and time, and you are responsible for charges until `runnerkit destroy --repo owner/name` verifies cleanup.", and `Ephemeral mode is not a fleet manager`.
    - `go test ./... && grep -R "persistent self-hosted runners" README.md docs && grep -R "Ephemeral mode is not a fleet manager" README.md docs && grep -R "Estimated cost is approximate. Hetzner pricing varies by region and time" README.md docs` exits 0.</acceptance_criteria>
  <done>Phase 5 has full fake regression coverage for safety docs, explicit mode selection, lifecycle status, log preservation, cleanup ordering, and no-autoscaling copy.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go test ./internal/cli/... && grep -R "runnerkit up --repo owner/name --mode ephemeral --cloud hetzner" README.md docs && grep -R "Self-hosted Runner Safety Guide" README.md docs/safety.md` exits 0.
- [ ] `go test ./internal/cli/... ./internal/bootstrap/... ./internal/provider/... ./internal/state/...` exits 0.
- [ ] `go test ./... && grep -R "persistent self-hosted runners" README.md docs && grep -R "Ephemeral mode is not a fleet manager" README.md docs && grep -R "Estimated cost is approximate. Hetzner pricing varies by region and time" README.md docs` exits 0.
- [ ] `grep -R "# Self-hosted Runner Safety Guide" docs/safety.md` returns a match.
- [ ] `grep -R "BYO ephemeral mode is a one-job GitHub registration, not a clean virtual machine." README.md docs` returns matches.
- [ ] `grep -R "Billing stops only after \`runnerkit destroy --repo owner/name\` verifies cleanup." README.md docs` returns matches.
- [ ] `grep -R "Estimated cost is approximate. Hetzner pricing varies by region and time" README.md docs` returns matches.
- [ ] `grep -R "No RunnerKit-managed runner is saved for" internal/cli/*_test.go` returns matches.
- [ ] `grep -R "Destroy ephemeral cloud runner: type" internal/cli/*_test.go` returns matches.
- [ ] `grep -R "TestEphemeralCloudRecommendedForPublicRepo" internal/cli/up_ephemeral_e2e_test.go` returns a match.
- [ ] `grep -R "ephemeral.logs.preserve" internal/cli/*_test.go` returns matches.
- [ ] `go test ./...` exits 0.
</verification>

<dependencies>
- Depends on 05-02 because docs and E2E tests validate actual ephemeral setup, state, status, logs, and cleanup behavior.
- Completes Phase 5 requirement DOC-03 and reinforces RUN-02/RUN-04 with regression tests and user-facing safety copy.
- Feeds Phase 6 release validation by leaving README/docs and fake E2E commands ready for v1 full-suite verification.
</dependencies>

<success_criteria>
- DOC-03 is covered because `docs/safety.md`, README, BYO quickstart, and cloud quickstart explain when persistent self-hosted runners are unsafe and when ephemeral mode is recommended.
- RUN-04 is reinforced because docs and tests preserve persistent-vs-ephemeral cost, isolation, cleanup, operations, and logs tradeoff copy.
- RUN-02 is reinforced because fake E2E tests prove explicit ephemeral BYO/cloud choices work and cleanup/log lifecycle surfaces are test-covered.
</success_criteria>

<output>
After completion, create `.planning/phases/05-scoped-ephemeral-mode-and-safety-profiles/05-03-SUMMARY.md`.
</output>
